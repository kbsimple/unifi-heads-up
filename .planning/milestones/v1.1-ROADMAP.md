# Milestone v1.1: Dev Mocking

**Status:** ✅ SHIPPED 2026-04-19
**Phases:** 5
**Total Plans:** 2

## Overview

Make the app fully usable in development without a real UniFi console by introducing a mock layer activated via `UNIFI_MOCK=true`. Covers all 8 MOCK requirements — mock data, facade switcher, dev.sh wiring, and test coverage.

## Phases

### Phase 5: Dev Mock Layer

**Goal**: Developers can run the app locally without a real UniFi console and exercise all dashboard features with realistic mock data
**Depends on**: Nothing (additive layer on top of v1.0 codebase)
**Plans**: 2 plans

Plans:

- [x] 05-01: Create mock.ts + index.ts facade, update API route imports and dev.sh
- [x] 05-02: Write mock unit tests and repair route test vi.mock targets

**Details:**

Requirements addressed: MOCK-01, MOCK-02, MOCK-03, MOCK-04, MOCK-05, MOCK-06, MOCK-07, MOCK-08

Files created:
- `src/lib/unifi/mock.ts` — 6 mock NetworkClients (High/Medium/Low x2/Idle x2) and 3 FirewallPolicies with in-memory toggle state
- `src/lib/unifi/index.ts` — 15-line facade that switches between mock and real client based on `UNIFI_MOCK=true`
- `tests/lib/unifi/mock.test.ts` — 11 tests covering all MOCK-04–MOCK-08 requirements
- `tests/lib/unifi/index.test.ts` — 4 facade smoke tests with vi.mock hoisting

Files modified:
- `src/app/api/clients/route.ts` — imports from `@/lib/unifi` (facade) instead of client directly
- `src/app/api/firewall/route.ts` — same facade import fix
- `dev.sh` — `export UNIFI_MOCK=true` added before `npm run dev`
- `tests/app/api/clients/route.test.ts` — vi.mock target repaired to `@/lib/unifi`
- `tests/app/api/firewall/route.test.ts` — same repair

---

## Milestone Summary

**Key Decisions:**

- Facade (index.ts) evaluates UNIFI_MOCK once at module init, not per-request — server restart required to flip mode (intentional)
- mock.ts has no server-only import — the server boundary is enforced by index.ts which imports both
- In-memory firewall toggle state uses module-level let variable — resets on HMR/server restart (intentional per MOCK-05)
- Toggle tests are self-contained: read current state, flip, assert, restore — avoids beforeEach reset complexity
- UNIFI_MOCK=true branch not tested via module reload — complexity not justified; end-to-end coverage via dev.sh is sufficient

**Issues Resolved:**

- Route tests broke when API routes switched from `@/lib/unifi/client` to `@/lib/unifi` — repaired vi.mock targets in 05-02
- 3 pre-existing test failures (dashboard/firewall page tests referencing non-existent `(dashboard)` path) — fixed in same session

**Issues Deferred:**

None.

**Technical Debt Incurred:**

None.

---

*For current project status, see .planning/ROADMAP.md*
