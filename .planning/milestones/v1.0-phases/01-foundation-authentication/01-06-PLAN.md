---
phase: 01-foundation-authentication
plan: 06
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/page.tsx
  - src/components/logout-button.tsx
autonomous: true
requirements: [AUTH-03]
user_setup: []

must_haves:
  truths:
    - "User sees top navigation bar with app name and logout button"
    - "User can click logout button to sign out"
    - "Dashboard layout accommodates future content (Phase 2)"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with navigation"
      contains: "Unifi Dashboard"
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Dashboard placeholder page"
      contains: "Welcome"
    - path: "src/components/logout-button.tsx"
      provides: "Logout button component"
      contains: "logout"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/logout-button.tsx"
      via: "import LogoutButton"
      pattern: "LogoutButton"
    - from: "src/components/logout-button.tsx"
      to: "src/app/actions/auth.ts"
      via: "logout Server Action"
      pattern: "logout"
---

<objective>
Create the dashboard layout with navigation bar and logout functionality. This implements AUTH-03 (logout) and D-09/D-10 (app shell).

Purpose: Provide authenticated app shell with navigation and logout capability.
Output: Dashboard layout, placeholder dashboard page, logout button component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
From src/app/actions/auth.ts:
```typescript
export async function logout(): Promise<void>
```

From src/components/ui/:
```typescript
// Button component from shadcn
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout with navigation</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/(dashboard)/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-authentication/01-UI-SPEC.md (for nav bar specs: 64px height, zinc-900 bg)
    - .planning/phases/01-foundation-authentication/01-CONTEXT.md (for D-09, D-10)
    - src/components/ui/button.tsx (for Button component)
  </read_first>
  <behavior>
    - Test 1: Dashboard layout renders navigation bar
    - Test 2: Navigation bar shows "Unifi Dashboard" on left
    - Test 3: Navigation bar has logout button on right
  </behavior>
  <action>
1. Create dashboard route group directory:
   Run: `mkdir -p src/app/\(dashboard\)`

2. Create src/app/(dashboard)/layout.tsx with navigation:
   ```tsx
   // src/app/(dashboard)/layout.tsx
   import { LogoutButton } from '@/components/logout-button'

   /**
    * Dashboard layout with navigation bar
    * Per D-09: Top navigation bar with app name left, logout right
    * Per D-10: Simple layout for future dashboard content
    */
   export default function DashboardLayout({
     children,
   }: {
     children: React.ReactNode
   }) {
     return (
       <div className="min-h-screen bg-zinc-950">
         {/* Navigation bar - 64px height per UI-SPEC */}
         <header className="h-16 bg-zinc-900 border-b border-zinc-800 px-6 flex items-center justify-between">
           {/* App name - left aligned */}
           <div className="flex items-center gap-2">
             <h1 className="text-xl font-semibold text-zinc-100">
               Unifi Dashboard
             </h1>
           </div>

           {/* Logout button - right aligned */}
           <LogoutButton />
         </header>

         {/* Main content area */}
         <main className="p-6">
           {children}
         </main>
       </div>
     )
   }
   ```

3. Create src/app/(dashboard)/page.tsx as placeholder:
   ```tsx
   // src/app/(dashboard)/page.tsx
   import { verifySession } from '@/lib/dal'

   /**
    * Dashboard placeholder page
    * Phase 2 will add device list and traffic monitoring
    */
   export default async function DashboardPage() {
     // Verify session (redirects to login if not authenticated)
     const { username } = await verifySession()

     return (
       <div className="space-y-4">
         <h2 className="text-2xl font-semibold text-zinc-100">
           Welcome, {username}
         </h2>
         <p className="text-zinc-400">
           Dashboard content will be added in Phase 2 (Device List & Traffic Monitoring).
         </p>
         <div className="mt-8 p-8 rounded-lg bg-zinc-900 border border-zinc-800">
           <p className="text-zinc-500 text-center">
             Device list and traffic monitoring coming soon...
           </p>
         </div>
       </div>
     )
   }
   ```

4. Verify TypeScript compilation:
   Run: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>
      test -f src/app/\(dashboard\)/layout.tsx && \
      test -f src/app/\(dashboard\)/page.tsx && \
      grep -q "Unifi Dashboard" src/app/\(dashboard\)/layout.tsx && \
      grep -q "LogoutButton" src/app/\(dashboard\)/layout.tsx && \
      grep -q "h-16" src/app/\(dashboard\)/layout.tsx && \
      grep -q "bg-zinc-900" src/app/\(dashboard\)/layout.tsx && \
      grep -q "verifySession" src/app/\(dashboard\)/page.tsx && \
      echo "PASS: Dashboard layout created"
    </automated>
  </verify>
  <done>
    src/app/(dashboard)/layout.tsx provides navigation bar
    Navigation bar is 64px tall with zinc-900 background
    App name "Unifi Dashboard" displayed on left
    LogoutButton component on right
    Main content area has padding for future content
    Dashboard page uses verifySession for auth check
    Placeholder content indicates Phase 2 additions
    TypeScript compilation succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create logout button component</name>
  <files>src/components/logout-button.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-authentication/01-RESEARCH.md (for logout button pattern)
    - .planning/phases/01-foundation-authentication/01-UI-SPEC.md (for "Sign out" copy)
    - src/app/actions/auth.ts (for logout Server Action)
    - src/components/ui/button.tsx (for Button component)
  </read_first>
  <behavior>
    - Test 1: Logout button renders "Sign out" text
    - Test 2: Logout button triggers logout Server Action on click
  </behavior>
  <action>
1. Create src/components directory if needed:
   Run: `mkdir -p src/components`

2. Create src/components/logout-button.tsx:
   ```tsx
   // src/components/logout-button.tsx
   'use client'

   import { logout } from '@/app/actions/auth'
   import { Button } from '@/components/ui/button'

   /**
    * Logout button component
    * Per AUTH-03: User can log out from any page
    * Per UI-SPEC: "Sign out" copy, ghost variant
    */
   export function LogoutButton() {
     return (
       <form action={logout}>
         <Button
           type="submit"
           variant="ghost"
           className="text-zinc-300 hover:text-zinc-100 hover:bg-zinc-800"
         >
           Sign out
         </Button>
       </form>
     )
   }
   ```

3. Update tests/logout.test.ts with component tests:
   ```typescript
   // tests/logout.test.ts
   import { describe, it, expect, vi } from 'vitest'
   import { render, screen } from '@testing-library/react'
   import { LogoutButton } from '@/components/logout-button'

   // Mock next/navigation
   vi.mock('next/navigation', () => ({
     redirect: vi.fn((path: string) => {
       throw new Error(`Redirect to ${path}`)
     }),
   }))

   vi.mock('next/headers', () => ({
     cookies: vi.fn(() =>
       Promise.resolve({
         delete: vi.fn(),
       })
     ),
   }))

   describe('Logout (AUTH-03)', () => {
     it('renders "Sign out" button', () => {
       render(<LogoutButton />)
       expect(screen.getByText('Sign out')).toBeInTheDocument()
     })

     it('has form action pointing to logout', () => {
       render(<LogoutButton />)
       const form = screen.getByRole('form')
       expect(form).toHaveAttribute('action')
     })
   })
   ```

4. Run tests:
   Run: `npm test --run tests/logout.test.ts`

5. Verify TypeScript compilation:
   Run: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>
      test -f src/components/logout-button.tsx && \
      grep -q "'use client'" src/components/logout-button.tsx && \
      grep -q "import { logout }" src/components/logout-button.tsx && \
      grep -q "Sign out" src/components/logout-button.tsx && \
      grep -q "variant=\"ghost\"" src/components/logout-button.tsx && \
      echo "PASS: Logout button created"
    </automated>
  </verify>
  <done>
    src/components/logout-button.tsx exports LogoutButton
    Button uses 'use client' directive
    Form action triggers logout Server Action
    Button shows "Sign out" text
    Ghost variant with zinc color scheme
    All tests pass
    TypeScript compilation succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | Logout form submission |
| Session → Application | Session verification before page render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Session | mitigate | verifySession checks before rendering dashboard |
| T-06-02 | Information Disclosure | Dashboard content | mitigate | verifySession redirects unauthenticated users |

</threat_model>

<verification>
1. Run `npm test --run tests/logout.test.ts` - all tests pass
2. Run `npx tsc --noEmit` - no type errors
3. Start dev server and verify:
   - Login as admin
   - See dashboard with navigation
   - Click "Sign out"
   - Verify redirect to /login
</verification>

<success_criteria>
- [ ] src/app/(dashboard)/layout.tsx exists with navigation bar
- [ ] Navigation bar shows "Unifi Dashboard" on left
- [ ] Navigation bar includes LogoutButton on right
- [ ] src/app/(dashboard)/page.tsx exists with placeholder content
- [ ] src/components/logout-button.tsx exists with "Sign out" button
- [ ] LogoutButton uses logout Server Action
- [ ] verifySession called in dashboard page
- [ ] All tests pass
- [ ] TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-06-SUMMARY.md`
</output>