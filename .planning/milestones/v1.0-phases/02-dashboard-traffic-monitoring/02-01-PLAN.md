---
phase: 02-dashboard-traffic-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/unifi/types.ts
  - src/lib/unifi/traffic.ts
  - src/lib/unifi/client.ts
  - src/app/api/clients/route.ts
autonomous: true
requirements: [DEVI-02, UIUX-02, UIUX-05]
user_setup:
  - service: UniFi Site Manager API
    why: Network client data source
    env_vars:
      - name: UNIFI_CONSOLE_ID
        source: "Navigate to unifi.ui.com/consoles/{console_id}/network - copy console_id from URL"
      - name: UNIFI_API_KEY
        source: "UniFi Site Manager Dashboard -> API Keys -> Create API Key"
    dashboard_config: []

must_haves:
  truths:
    - "SWR is installed and available for client-side data fetching"
    - "shadcn badge, skeleton, alert components exist in src/components/ui/"
    - "UniFi types define NetworkClient with all required fields"
    - "Traffic calculation returns correct status based on bandwidth"
    - "API route /api/clients returns client data or error response"
  artifacts:
    - path: "package.json"
      provides: "SWR dependency for polling"
      contains: '"swr"'
    - path: "src/lib/unifi/types.ts"
      provides: "TypeScript types for UniFi responses"
      exports: ["NetworkClient", "ClientsResponse", "UnifiClientSchema"]
    - path: "src/lib/unifi/traffic.ts"
      provides: "Traffic status calculation"
      exports: ["calculateTrafficStatus", "bytesPerSecToMbps"]
    - path: "src/lib/unifi/client.ts"
      provides: "UniFi Site Manager API client"
      exports: ["getUnifiClients"]
    - path: "src/app/api/clients/route.ts"
      provides: "API endpoint for SWR"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/clients/route.ts"
      to: "src/lib/unifi/client.ts"
      via: "import getUnifiClients"
      pattern: "import.*getUnifiClients"
    - from: "src/lib/unifi/client.ts"
      to: "src/lib/unifi/types.ts"
      via: "import types"
      pattern: "import.*NetworkClient"
---

<objective>
Install data fetching dependencies and create the UniFi API infrastructure layer for Phase 2.

Purpose: Establish the backend foundation (types, client, API route) that dashboard components will consume.
Output: SWR installed, shadcn components added, UniFi types, traffic calculation, API client, and API route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md
@.planning/phases/02-dashboard-traffic-monitoring/02-UI-SPEC.md
@.planning/phases/01-foundation-authentication/01-06-SUMMARY.md
@src/lib/definitions.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/definitions.ts:
```typescript
export const ERROR_MESSAGES: Record<string, string> = {
  INVALID_CREDENTIALS: 'Invalid username or password',
  SESSION_EXPIRED: 'Your session has expired. Please sign in again.',
  NETWORK_ERROR: 'Unable to connect. Please check your connection and try again.',
  UNAUTHORIZED: 'You must be signed in to access this page.',
  UNKNOWN: 'An unexpected error occurred. Please try again.',
}

export function getErrorMessage(code: string): string
```

From src/lib/dal.ts:
```typescript
export const verifySession = cache(async () => {
  // Returns { isAuth: true, username: string } or redirects to /login
})
```

From src/lib/session.ts:
```typescript
export async function getSession(): Promise<SessionPayload | null>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and shadcn components</name>
  <files>package.json, src/components/ui/badge.tsx, src/components/ui/skeleton.tsx, src/components/ui/alert.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/package.json
    - /Users/ffaber/claude-projects/unifi-api/components.json
  </read_first>
  <action>
Install SWR for client-side polling and add shadcn components for Phase 2 UI states.

1. Install SWR:
   ```bash
   npm install swr
   ```

2. Add shadcn components (badge for traffic status, skeleton for loading, alert for errors):
   ```bash
   pnpm dlx shadcn@latest add badge skeleton alert
   ```

3. Verify components exist in src/components/ui/ after installation.

Per UI-SPEC.md: badge uses outline variant, skeleton for loading states, alert for error/offline states.
  </action>
  <verify>
    <automated>grep -q '"swr"' /Users/ffaber/claude-projects/unifi-api/package.json && test -f /Users/ffaber/claude-projects/unifi-api/src/components/ui/badge.tsx && test -f /Users/ffaber/claude-projects/unifi-api/src/components/ui/skeleton.tsx && test -f /Users/ffaber/claude-projects/unifi-api/src/components/ui/alert.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - SWR listed in package.json dependencies
    - Badge component exists at src/components/ui/badge.tsx
    - Skeleton component exists at src/components/ui/skeleton.tsx
    - Alert component exists at src/components/ui/alert.tsx
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create UniFi types and traffic calculation</name>
  <files>src/lib/unifi/types.ts, src/lib/unifi/traffic.ts, tests/lib/unifi/traffic.test.ts</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md (Pattern 2, Pattern 3)
    - /Users/ffaber/claude-projects/unifi-api/vitest.config.ts
  </read_first>
  <behavior>
    - Test 1: calculateTrafficStatus(0, 0) returns 'idle' (less than 1 Mbps)
    - Test 2: calculateTrafficStatus(125000, 125000) returns 'low' (2 Mbps total)
    - Test 3: calculateTrafficStatus(6250000, 6250000) returns 'medium' (100 Mbps total)
    - Test 4: calculateTrafficStatus(12500000, 12500000) returns 'high' (200 Mbps total)
    - Test 5: bytesPerSecToMbps(125000) returns 1 (1 Mbps)
  </behavior>
  <action>
Create UniFi types and traffic calculation logic following RESEARCH.md patterns.

1. Create src/lib/unifi/types.ts with:
   - UnifiClientSchema (Zod schema for API response validation)
   - UnifiClient type (raw API response shape)
   - NetworkClient interface (transformed for UI consumption):
     - id: string
     - mac: string
     - displayName: string (per DEVI-03: name ?? hostname ?? mac)
     - ip: string | null
     - lastSeen: Date | null (per DEVI-04)
     - isWired: boolean
     - isGuest: boolean
     - downloadRate: number (bytes/sec)
     - uploadRate: number (bytes/sec)
     - trafficStatus: 'idle' | 'low' | 'medium' | 'high'
   - ClientsResponse interface with clients array and timestamp

2. Create src/lib/unifi/traffic.ts with:
   - TRAFFIC_THRESHOLDS constant (IDLE: 1, LOW: 10, MEDIUM: 100 Mbps)
   - bytesPerSecToMbps(bytesPerSec: number): number
   - calculateTrafficStatus(download: number, upload: number): status
   - Follow RESEARCH.md Pattern 3 exactly

3. Create tests/lib/unifi/traffic.test.ts with unit tests for traffic calculation.

4. Export all types from src/lib/unifi/types.ts.

Per RESEARCH.md: Thresholds are Idle < 1 Mbps, Low 1-10 Mbps, Medium 10-100 Mbps, High > 100 Mbps.
  </action>
  <verify>
    <automated>pnpm test:run -- traffic.test.ts 2>&1 | grep -E "(PASS|Tests:.*passed)" || echo "Tests not passing"</automated>
  </verify>
  <done>
    - src/lib/unifi/types.ts exports UnifiClientSchema, NetworkClient, ClientsResponse
    - src/lib/unifi/traffic.ts exports calculateTrafficStatus, bytesPerSecToMbps
    - All traffic calculation tests pass
    - TypeScript compilation succeeds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create UniFi API client and API route</name>
  <files>src/lib/unifi/client.ts, src/app/api/clients/route.ts, tests/lib/unifi/client.test.ts, tests/app/api/clients/route.test.ts</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md (Pattern 1, Pattern 2)
    - /Users/ffaber/claude-projects/unifi-api/src/lib/dal.ts
    - /Users/ffaber/claude-projects/unifi-api/src/lib/definitions.ts
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/types.ts (from Task 2)
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/traffic.ts (from Task 2)
  </read_first>
  <behavior>
    - Test 1: getUnifiClients returns NetworkClient array with transformed data
    - Test 2: getUnifiClients handles API error and throws
    - Test 3: API route GET returns 200 with clients data
    - Test 4: API route GET returns 503 on network error
    - Test 5: API route GET returns 401 when no session
  </behavior>
  <action>
Create UniFi Site Manager API client and Next.js API route.

1. Create src/lib/unifi/client.ts with:
   - 'server-only' guard at top
   - Import ky for HTTP requests
   - getUnifiClients() function:
     - Uses UNIFI_CONSOLE_ID and UNIFI_API_KEY env vars
     - Calls Site Manager Proxy endpoint: `https://api.ui.com/proxy/network/v2/api/site/default/stat/sta`
     - Headers: { 'X-API-KEY': API_KEY, 'Content-Type': 'application/json' }
     - 10 second timeout
     - Validates response with UnifiClientSchema.array().parse()
     - Transforms to NetworkClient[] with displayName fallback chain
     - Calculates trafficStatus for each client
     - Returns ClientsResponse with timestamp
   - Follow RESEARCH.md Pattern 2 exactly

2. Create src/app/api/clients/route.ts with:
   - GET handler that:
     - Verifies session with getSession() (returns 401 if no session)
     - Calls getUnifiClients()
     - Returns NextResponse.json(data) on success
     - Returns structured error on failure (per UIUX-05):
       - 503 for network errors
       - 500 for API errors
     - Uses ERROR_MESSAGES for user-friendly messages
   - Follow RESEARCH.md Code Examples section

3. Create tests with ky mocking for API client tests.

CRITICAL: API key must NEVER be prefixed with NEXT_PUBLIC_ - use server-only env vars only.
Per RESEARCH.md Anti-Patterns: "Storing API key in NEXT_PUBLIC_ env vars would expose UniFi API key in client bundle."
  </action>
  <verify>
    <automated>pnpm test:run -- client.test.ts route.test.ts 2>&1 | grep -E "(PASS|Tests:.*passed)" || echo "Tests not passing"</automated>
  </verify>
  <done>
    - src/lib/unifi/client.ts exports getUnifiClients (server-only)
    - src/app/api/clients/route.ts exports GET handler
    - API route returns 401 without session
    - API route returns 503 on network failure (UIUX-05)
    - All client and route tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API Route /api/clients | Untrusted input, session required |
| API Route → UniFi Site Manager API | External service, HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | client.ts | mitigate | Store UNIFI_API_KEY in server-only env vars, never NEXT_PUBLIC_ |
| T-02-02 | Tampering | client.ts | mitigate | Validate all API responses with Zod schema before use |
| T-02-03 | Denial of Service | API route | accept | SWR deduplication + 60s polling interval prevents rate limiting |
| T-02-04 | Elevation of Privilege | API route | mitigate | Verify session via getSession() before returning data |
</threat_model>

<verification>
1. Run `pnpm test:run` - all tests pass
2. Run `npx tsc --noEmit` - no TypeScript errors
3. Verify SWR in package.json
4. Verify badge.tsx, skeleton.tsx, alert.tsx exist
5. Verify src/lib/unifi/ directory exists with types.ts, traffic.ts, client.ts
6. Verify src/app/api/clients/route.ts exists
</verification>

<success_criteria>
- SWR dependency installed
- shadcn badge, skeleton, alert components added
- UniFi types define NetworkClient with all required fields (DEVI-01, DEVI-03, DEVI-04)
- Traffic calculation correctly determines status (DEVI-02, UIUX-02)
- API client fetches from Site Manager Proxy
- API route returns client data or appropriate error (UIUX-05)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-traffic-monitoring/02-01-SUMMARY.md`
</output>