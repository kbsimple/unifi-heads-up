---
phase: 04-enhanced-features
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/groups/device-chip.tsx
  - src/components/groups/group-card.tsx
  - src/components/groups/group-list.tsx
  - src/components/groups/create-group-modal.tsx
  - src/app/(dashboard)/groups/page.tsx
autonomous: true
requirements:
  - GRUP-01
  - GRUP-02
  - GRUP-03
  - GRUP-04
  - GTRA-01
  - GTRA-02
must_haves:
  truths:
    - "User can create a new group with a custom name"
    - "User can add devices to a group via checkbox selection"
    - "User can remove a device from a group via X button"
    - "User can delete a group"
    - "User sees aggregated traffic status (High/Medium/Low/Idle) for each group"
    - "User sees individual device status badges when group is expanded"
  artifacts:
    - path: "src/app/(dashboard)/groups/page.tsx"
      provides: "Groups route page"
      contains: "GroupList"
    - path: "src/components/groups/create-group-modal.tsx"
      provides: "Group creation dialog"
      exports: ["CreateGroupModal"]
    - path: "src/components/groups/group-card.tsx"
      provides: "Individual group display with expand/collapse"
      contains: "TrafficBadge"
    - path: "src/components/groups/device-chip.tsx"
      provides: "Device pill with remove button"
      exports: ["DeviceChip"]
  key_links:
    - from: "src/components/groups/group-card.tsx"
      to: "src/hooks/use-groups.ts"
      via: "useGroups hook"
      pattern: "useGroups"
    - from: "src/components/groups/create-group-modal.tsx"
      to: "src/hooks/use-groups.ts"
      via: "createGroup function"
      pattern: "createGroup"
---

<objective>
Build the Groups UI with card display, creation modal, and device management.

Purpose: Deliver the complete groups feature - users can create, view, edit, and delete device groups with traffic aggregation.
Output: Working Groups page with full CRUD UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enhanced-features/04-CONTEXT.md
@.planning/phases/04-enhanced-features/04-RESEARCH.md
@.planning/phases/04-enhanced-features/04-UI-SPEC.md
@.planning/phases/04-enhanced-features/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/types/group.ts (created in Plan 01):
```typescript
export interface DeviceGroup {
  id: string
  name: string
  deviceIds: string[]
}
```

From src/hooks/use-groups.ts (created in Plan 01):
```typescript
export function useGroups(): {
  groups: DeviceGroup[]
  createGroup: (name: string) => void
  addGroupDevice: (groupId: string, deviceId: string) => void
  removeGroupDevice: (groupId: string, deviceId: string) => void
  deleteGroup: (groupId: string) => void
}
```

From src/lib/unifi/types.ts:
```typescript
export interface NetworkClient {
  id: string
  mac: string
  displayName: string
  ip: string | null
  lastSeen: Date | null
  isWired: boolean
  isGuest: boolean
  downloadRate: number
  uploadRate: number
  trafficStatus: 'idle' | 'low' | 'medium' | 'high'
}
```

From src/components/dashboard/traffic-badge.tsx:
```typescript
export function TrafficBadge({ status }: { status: 'idle' | 'low' | 'medium' | 'high' })
```

From src/components/ui/dialog.tsx (installed in Plan 01):
```typescript
export function Dialog(props)
export function DialogTrigger(props)
export function DialogContent(props)
export function DialogHeader(props)
export function DialogTitle(props)
export function DialogDescription(props)
```

From src/components/ui/checkbox.tsx (installed in Plan 01):
```typescript
export function Checkbox(props)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeviceChip component</name>
  <files>src/components/groups/device-chip.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/traffic-badge.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/types.ts
  </read_first>
  <action>
    Create `src/components/groups/device-chip.tsx` per D-07 and UI-SPEC Device Chip Pattern.

    Requirements:
    - Client component (add 'use client' directive)
    - Props interface:
      ```typescript
      interface DeviceChipProps {
        device: NetworkClient
        onRemove: () => void
      }
      ```
    - Display device name (use device.displayName)
    - Show traffic status badge next to name
    - Remove button (X icon from lucide-react) with:
      - `aria-label="Remove {deviceName} from group"`
      - Hover state: `text-zinc-500 hover:text-red-400`
    - Pill styling: `rounded-full bg-zinc-800 px-3 py-1`
    - Flex layout with gap between name and remove button

    Import X icon from lucide-react.
  </action>
  <acceptance_criteria>
    - File exists: src/components/groups/device-chip.tsx
    - Contains 'use client' directive
    - Exports DeviceChip component
    - Accepts device: NetworkClient and onRemove: () => void props
    - Uses TrafficBadge component
    - Contains X icon from lucide-react
    - Has aria-label on remove button
    - Has rounded-full, bg-zinc-8 classes
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -E "DeviceChip|onRemove|aria-label|rounded-full" src/components/groups/device-chip.tsx</manual>
  </verify>
  <done>
    DeviceChip component displays device name with badge and remove button, styled as a pill.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GroupCard component</name>
  <files>src/components/groups/group-card.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/components/firewall/firewall-card.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/traffic-badge.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/hooks/use-groups.ts
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/types.ts
    - /Users/ffaber/claude-projects/unifi-api/src/components/groups/device-chip.tsx
  </read_first>
  <action>
    Create `src/components/groups/group-card.tsx` per D-03, D-06, D-07, D-10, D-11, D-12.

    Requirements:
    - Client component ('use client')
    - Props:
      ```typescript
      interface GroupCardProps {
        group: DeviceGroup
        devices: NetworkClient[]
        onRemoveDevice: (groupId: string, deviceId: string) => void
        onDelete: (groupId: string) => void
        onAddDevices: (groupId: string) => void
      }
      ```
    - Card structure matching Phase 2/3 pattern: `bg-zinc-900 border-zinc-800 rounded-lg`
    - Header row:
      - Group name (left): `font-medium text-zinc-100`
      - Device count: `text-sm text-zinc-500` in parentheses
      - Aggregated traffic badge (per D-10): Compute by summing device download/upload rates
      - Delete button (X icon) with `aria-label="Delete {groupName} group"`
    - Expandable section (click group name to toggle):
      - Per D-11: Show DeviceChip for each device in group with traffic badge
      - Per D-12: Empty state "No devices in this group" when deviceIds is empty
      - "Add devices" button when expanded
    - Traffic aggregation logic:
      - Sum downloadRate + uploadRate for all devices in group
      - Calculate status: High (&gt;1MB/s), Medium (&gt;100KB/s), Low (&gt;0), Idle (0)

    Import Trash2 or X icon from lucide-react for delete button.
    Use useState for expanded state.
  </action>
  <acceptance_criteria>
    - File exists: src/components/groups/group-card.tsx
    - Contains 'use client' directive
    - Exports GroupCard component
    - Uses Card, CardContent from ui/card
    - Uses TrafficBadge component
    - Uses DeviceChip component
    - Has useState for expanded state
    - Computes aggregated traffic status from device rates
    - Shows device chips in expanded view
    - Shows "No devices" placeholder when empty
    - Has delete button with aria-label
    - Has expand/collapse on group name click
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -E "GroupCard|expanded|aggregated|TrafficBadge|DeviceChip|No devices" src/components/groups/group-card.tsx</manual>
  </verify>
  <done>
    GroupCard shows group name, device count, aggregated traffic status, and expandable device list with per-device badges.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CreateGroupModal component</name>
  <files>src/components/groups/create-group-modal.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/dialog.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/checkbox.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/button.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/input.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/types.ts
  </read_first>
  <action>
    Create `src/components/groups/create-group-modal.tsx` per D-04, D-05.

    Requirements:
    - Client component ('use client')
    - Props:
      ```typescript
      interface CreateGroupModalProps {
        devices: NetworkClient[]
        onGroupCreated: (groupId: string, deviceIds: string[]) => void
      }
      ```
    - Uses shadcn Dialog components (Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription)
    - Trigger button: "Create Group" (variant="outline" or default)
    - Dialog content:
      - Title: "Create Device Group"
      - Description: "Enter a name and select devices to add to this group."
      - Group name input (required, max 50 chars, Input component)
      - Device selection via Checkboxes (per D-05):
        - Label for each checkbox: device.displayName
        - aria-label for accessibility
        - Store selected device IDs in local state (array of strings)
      - Actions: "Cancel" (secondary) and "Create Group" (primary/sky-600)
    - On "Create Group" click:
      - Validate name is not empty
      - Call onGroupCreated with name and selected deviceIds
      - Close dialog
      - Reset form state
    - Dark theme styling: Dialog content uses `bg-zinc-900 border-zinc-800`

    Use useState for form state (name, selected device IDs, open state).
  </action>
  <acceptance_criteria>
    - File exists: src/components/groups/create-group-modal.tsx
    - Contains 'use client' directive
    - Exports CreateGroupModal component
    - Uses Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription
    - Uses Checkbox components for device selection
    - Uses Input for group name
    - Uses Button for trigger and actions
    - Has useState for selectedDeviceIds array
    - Has useState for groupName input
    - Has "Create Group" button that calls onGroupCreated
    - Has Cancel button
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -E "CreateGroupModal|Dialog|Checkbox|selectedDeviceIds|groupName|onGroupCreated" src/components/groups/create-group-modal.tsx</manual>
  </verify>
  <done>
    CreateGroupModal provides group creation with name input and checkbox device selection.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create GroupList and Groups page</name>
  <files>src/components/groups/group-list.tsx, src/app/(dashboard)/groups/page.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/client-list.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/groups/group-card.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/groups/create-group-modal.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/hooks/use-groups.ts
    - /Users/ffaber/claude-projects/unifi-api/src/app/(dashboard)/page.tsx
  </read_first>
  <action>
    Create two files:

    1. `src/components/groups/group-list.tsx`:
       - Client component ('use client')
       - Props:
         ```typescript
         interface GroupListProps {
           initialDevices: NetworkClient[]
         }
         ```
       - Fetch devices via SWR from '/api/clients' (same as client-list pattern)
       - Use useGroups hook for group management
       - Render CreateGroupModal at top
       - Render GroupCard for each group
       - Empty state when no groups: icon (FolderPlus from lucide), heading "No groups created yet", body "Create your first group to organize and monitor devices together."
       - Wire up handlers:
         - CreateGroupModal.onGroupCreated → createGroup + addGroupDevice for each selected
         - GroupCard.onRemoveDevice → removeGroupDevice
         - GroupCard.onDelete → deleteGroup

    2. `src/app/(dashboard)/groups/page.tsx`:
       - Server component
       - Verify session via verifySession()
       - Fetch initial clients via getUnifiClients()
       - Render GroupList with initial data
       - Page title: "Device Groups" (h2, text-2xl font-semibold text-zinc-100)
       - Space-y-4 layout

    Follow patterns from src/app/(dashboard)/page.tsx and client-list.tsx.
  </action>
  <acceptance_criteria>
    - File exists: src/components/groups/group-list.tsx
    - File exists: src/app/(dashboard)/groups/page.tsx
    - group-list.tsx has 'use client' directive
    - group-list.tsx exports GroupList component
    - group-list.tsx uses useSWR for /api/clients
    - group-list.tsx uses useGroups hook
    - group-list.tsx renders GroupCard components
    - group-list.tsx renders CreateGroupModal
    - group-list.tsx has empty state with "No groups created yet"
    - groups/page.tsx calls verifySession()
    - groups/page.tsx calls getUnifiClients()
    - groups/page.tsx renders GroupList with initialData
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -E "GroupList|useGroups|useSWR|No groups created yet" src/components/groups/group-list.tsx && grep -E "verifySession|getUnifiClients|GroupList" src/app/\(dashboard\)/groups/page.tsx</manual>
  </verify>
  <done>
    Groups page displays all groups with creation modal, device management, and traffic aggregation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dialog input | User-supplied group name enters the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Group name input | mitigate | Validate group name: max 50 chars, alphanumeric + spaces + hyphens only (Zod pattern from RESEARCH.md) |
| T-04-04 | Information Disclosure | Device IDs in localStorage | accept | Device IDs are not PII, only internal identifiers |
</threat_model>

<verification>
- Groups page loads at /groups route
- CreateGroupModal opens and creates groups
- GroupCard displays with aggregated traffic status
- DeviceChip shows device with remove button
- Groups persist in localStorage across refresh
- All existing tests pass
</verification>

<success_criteria>
- Groups page accessible via /groups route
- User can create groups with custom names
- User can add/remove devices from groups
- User can delete groups
- Group traffic status aggregates device rates correctly
- Per-device status visible in expanded view
- Empty states display correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhanced-features/04-02-SUMMARY.md`
</output>