---
phase: 260604-rg1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/groups/page.tsx
  - src/app/dashboard/layout.tsx
  - src/components/groups/add-devices-modal.tsx
  - src/components/groups/create-group-modal.tsx
  - src/components/groups/device-chip.tsx
  - src/components/groups/group-card.tsx
  - src/components/groups/group-list.tsx
  - src/hooks/use-groups.ts
  - src/hooks/use-local-storage.ts
  - src/lib/types/group.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "No group-related files exist in the codebase"
    - "The nav bar shows Dashboard, Firewall, and Insights — no Groups tab"
    - "The app builds without TypeScript errors"
    - "The localStorage key 'unifi-device-groups' has no writer anywhere in the codebase"
  artifacts:
    - path: "src/app/dashboard/layout.tsx"
      provides: "Nav with Groups link removed"
  key_links:
    - from: "src/app/dashboard/layout.tsx"
      to: "/dashboard/groups route"
      via: "Link href"
      pattern: "dashboard/groups"
---

<objective>
Remove the groups feature entirely — all components, hooks, types, routes, and UI references.

Purpose: Groups were stored in localStorage and never connected to UniFi APIs; the feature is dead weight. Removing it reduces confusion and maintenance surface.
Output: Clean codebase with no group code; nav shows Dashboard, Firewall, Insights only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all group files</name>
  <files>
    src/app/dashboard/groups/page.tsx,
    src/components/groups/add-devices-modal.tsx,
    src/components/groups/create-group-modal.tsx,
    src/components/groups/device-chip.tsx,
    src/components/groups/group-card.tsx,
    src/components/groups/group-list.tsx,
    src/hooks/use-groups.ts,
    src/hooks/use-local-storage.ts,
    src/lib/types/group.ts
  </files>
  <action>
    Delete every file listed above using the Bash tool:

    ```bash
    rm -rf src/app/dashboard/groups
    rm -rf src/components/groups
    rm src/hooks/use-groups.ts
    rm src/hooks/use-local-storage.ts
    rm src/lib/types/group.ts
    ```

    Note: `use-local-storage.ts` is used ONLY by `use-groups.ts` (confirmed by grep). Delete it as well.
    Note: `src/components/dashboard/client-table.tsx` contains the CSS class word "group" (Tailwind hover group utility) — do NOT touch that file.
    Note: `src/components/ui/` files contain the word "group" only as a Tailwind class — do NOT touch those files.
  </action>
  <verify>
    Run:
    ```bash
    ls src/components/groups 2>&1 | grep -c "No such file" && \
    ls src/app/dashboard/groups 2>&1 | grep -c "No such file" && \
    ls src/hooks/use-groups.ts 2>&1 | grep -c "No such file" && \
    ls src/hooks/use-local-storage.ts 2>&1 | grep -c "No such file" && \
    ls src/lib/types/group.ts 2>&1 | grep -c "No such file"
    ```
    Each command should output `1`.
  </verify>
  <done>All group files deleted; no group directory remains under src/components or src/app/dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Groups nav link and verify build</name>
  <files>src/app/dashboard/layout.tsx</files>
  <action>
    Edit `src/app/dashboard/layout.tsx` to remove the Groups navigation link entirely.

    Remove this block (lines ~53–62):
    ```tsx
    <Link
      href="/dashboard/groups"
      className={`text-sm font-medium transition-colors ${
        pathname === '/dashboard/groups'
          ? 'text-sky-600 border-b-2 border-sky-600'
          : 'text-zinc-400 hover:text-zinc-100'
      }`}
    >
      Groups
    </Link>
    ```

    After editing, the nav should contain exactly three links: Dashboard, Firewall, Insights.
    Also remove the comment referencing Groups from the nav comment block (e.g. "per D-01, D-02: Three navigation tabs (Dashboard, Firewall, Groups)").

    Then verify the TypeScript build passes:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    ```bash
    npx tsc --noEmit && echo "TYPE_CHECK_OK"
    grep -r "use-groups\|use-local-storage\|unifi-device-groups\|/dashboard/groups" src/ && echo "FOUND_REFS" || echo "NO_REFS_OK"
    ```
    Expected: `TYPE_CHECK_OK` and `NO_REFS_OK`.
  </verify>
  <done>Groups nav link removed, no remaining references to group code anywhere under src/, TypeScript reports zero errors.</done>
</task>

</tasks>

<verification>
- `src/components/groups/` directory does not exist
- `src/app/dashboard/groups/` directory does not exist
- `src/hooks/use-groups.ts` does not exist
- `src/hooks/use-local-storage.ts` does not exist
- `src/lib/types/group.ts` does not exist
- `src/app/dashboard/layout.tsx` contains no reference to `/dashboard/groups`
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
Zero group files in the codebase. Nav bar has exactly three tabs: Dashboard, Firewall, Insights. TypeScript build clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260604-rg1-remove-groups-feature/260604-rg1-SUMMARY.md` using the summary template.
</output>
