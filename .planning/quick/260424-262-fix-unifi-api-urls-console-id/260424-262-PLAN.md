---
phase: quick-260424-262
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/unifi/client.ts
autonomous: true
requirements:
  - QUICK-260424-262
must_haves:
  truths:
    - "All four UniFi API calls route through /ea/console/{consoleId}/proxy/... URLs"
    - "The consoleId already validated in each function is actually used in the URL"
  artifacts:
    - path: "src/lib/unifi/client.ts"
      provides: "Corrected URL construction in all four functions"
      contains: "/ea/console/${consoleId}/proxy/"
  key_links:
    - from: "src/lib/unifi/client.ts"
      to: "https://api.ui.com/ea/console/{consoleId}/proxy/network/..."
      via: "ky HTTP calls"
      pattern: "ea/console/\\$\\{consoleId\\}/proxy"
---

<objective>
Insert `/ea/console/${consoleId}` into every URL built in `src/lib/unifi/client.ts`.

Purpose: The UniFi Site Manager Proxy requires the console ID in the path. Without it, every API call hits a non-existent route and returns 404/401.
Output: `src/lib/unifi/client.ts` with all four URL strings corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/unifi/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all four URL constructions to include consoleId path segment</name>
  <files>src/lib/unifi/client.ts</files>
  <action>
In `src/lib/unifi/client.ts`, change the URL string in every `ky.get` / `ky.put` call so that `/proxy/...` becomes `/ea/console/${consoleId}/proxy/...`.

The four changes (each is a one-line edit):

1. `getUnifiClients` (line ~60):
   - Before: `${SITE_MANAGER_BASE}/proxy/network/v2/api/site/default/stat/sta`
   - After:  `${SITE_MANAGER_BASE}/ea/console/${consoleId}/proxy/network/v2/api/site/default/stat/sta`

2. `isZoneBasedFirewallEnabled` (line ~104):
   - Before: `${SITE_MANAGER_BASE}/proxy/network/v2/api/site/default/site-feature-migration`
   - After:  `${SITE_MANAGER_BASE}/ea/console/${consoleId}/proxy/network/v2/api/site/default/site-feature-migration`

3. `getFirewallPolicies` (line ~133):
   - Before: `${SITE_MANAGER_BASE}/proxy/network/v2/api/site/default/firewall-policies`
   - After:  `${SITE_MANAGER_BASE}/ea/console/${consoleId}/proxy/network/v2/api/site/default/firewall-policies`

4. `updateFirewallPolicy` (line ~166):
   - Before: `${SITE_MANAGER_BASE}/proxy/network/v2/api/site/default/firewall-policies/${policyId}`
   - After:  `${SITE_MANAGER_BASE}/ea/console/${consoleId}/proxy/network/v2/api/site/default/firewall-policies/${policyId}`

No other changes. No refactoring of the consoleId reads — they are already validated correctly in each function, they just need to be used in the URL template literal.
  </action>
  <verify>grep -n "ea/console" /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/client.ts | wc -l | xargs -I{} test {} -eq 4 && echo "PASS: 4 URLs corrected" || echo "FAIL: expected 4 occurrences"</verify>
  <done>All four URL template literals contain `/ea/console/${consoleId}/proxy/...`. Zero occurrences of the old pattern `${SITE_MANAGER_BASE}/proxy/` remain in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→UniFi API | API key sent in X-API-KEY header over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-01 | Information Disclosure | consoleId in URL | accept | consoleId is non-secret infrastructure ID, transmitted over HTTPS, not a credential |
| T-262-02 | Spoofing | UNIFI_API_KEY header | accept | Already validated present before use; transmitted over HTTPS only |
</threat_model>

<verification>
grep -c "ea/console/\${consoleId}" src/lib/unifi/client.ts
# Expected: 4

grep "/proxy/network" src/lib/unifi/client.ts | grep -v "ea/console"
# Expected: no output (no old-style URLs remain)
</verification>

<success_criteria>
- `src/lib/unifi/client.ts` contains exactly 4 occurrences of `/ea/console/${consoleId}/proxy/`
- No URL in the file still starts with `${SITE_MANAGER_BASE}/proxy/`
- TypeScript build passes: `npx tsc --noEmit`
</success_criteria>

<output>
After completion, create `.planning/quick/260424-262-fix-unifi-api-urls-console-id/260424-262-SUMMARY.md`
</output>
