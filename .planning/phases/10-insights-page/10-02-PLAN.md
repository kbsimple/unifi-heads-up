---
phase: 10-insights-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/layout.tsx
  - src/app/dashboard/insights/page.tsx
autonomous: true
requirements:
  - INS-01

must_haves:
  truths:
    - "Insights link appears in the main navigation bar alongside Dashboard, Firewall, Groups"
    - "Navigating to /dashboard/insights renders the page without error"
    - "Page shows loading skeleton placeholders while client components fetch data"
    - "Page title and section headings are present in the rendered HTML"
  artifacts:
    - path: "src/app/dashboard/layout.tsx"
      provides: "Nav bar with Insights link added"
      contains: "href=\"/dashboard/insights\""
    - path: "src/app/dashboard/insights/page.tsx"
      provides: "Server Component shell for Insights page"
      exports: ["default"]
  key_links:
    - from: "src/app/dashboard/layout.tsx"
      to: "src/app/dashboard/insights/page.tsx"
      via: "Next.js App Router href routing"
      pattern: "/dashboard/insights"
---

<objective>
Add the Insights nav link and create the Server Component page shell that will host
the client chart components from Plan 03.

Purpose: Establish the route and navigation entry point so the page is reachable before
client-side components are wired.
Output: Updated nav layout + bare Insights page with Suspense boundaries for client components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/dashboard/layout.tsx
</context>

<interfaces>
<!-- Existing nav pattern from src/app/dashboard/layout.tsx -->
```tsx
<Link
  href="/dashboard/firewall"
  className={`text-sm font-medium transition-colors ${
    pathname === '/dashboard/firewall'
      ? 'text-sky-600 border-b-2 border-sky-600'
      : 'text-zinc-400 hover:text-zinc-100'
  }`}
>
  Firewall
</Link>
```
<!-- Dark theme tokens: bg-zinc-950, bg-zinc-900, border-zinc-800, text-zinc-100, text-zinc-400 -->
<!-- Skeleton component available at: src/components/ui/skeleton.tsx -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Insights nav link to dashboard layout</name>
  <files>src/app/dashboard/layout.tsx</files>
  <action>
    Add a fourth nav link for Insights to the existing `<nav>` block in `src/app/dashboard/layout.tsx`.
    Place it after the Groups link. Follow the exact same pattern as the three existing links:
    active state uses `text-sky-600 border-b-2 border-sky-600`, inactive uses `text-zinc-400 hover:text-zinc-100`.

    The href must be `/dashboard/insights`. The active check: `pathname === '/dashboard/insights'` OR
    `pathname.startsWith('/dashboard/insights')` (to handle future sub-routes).

    Do not change any other part of the layout.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The Insights link renders in the nav bar. Active state highlights when on /dashboard/insights. No existing nav links are broken.</done>
</task>

<task type="auto">
  <name>Task 2: Insights page Server Component shell</name>
  <files>src/app/dashboard/insights/page.tsx</files>
  <action>
    Create `src/app/dashboard/insights/page.tsx` as a React Server Component (no `'use client'`).

    Structure:
    ```tsx
    import { Suspense } from 'react'
    import { Skeleton } from '@/components/ui/skeleton'

    export const metadata = { title: 'Insights — Unifi Dashboard' }

    export default function InsightsPage() {
      return (
        <div className="space-y-8">
          <div>
            <h1 className="text-2xl font-semibold text-zinc-100">Insights</h1>
            <p className="text-sm text-zinc-400 mt-1">
              Bandwidth usage and activity patterns across your devices
            </p>
          </div>

          {/* Time range selector — rendered by client component in Plan 03 */}
          {/* Placeholder div; InsightsShell replaces this in Plan 03 */}
          <div id="insights-root">
            {/* Top Devices section */}
            <section aria-label="Top Devices">
              <h2 className="text-lg font-medium text-zinc-200 mb-4">Top Devices</h2>
              <Suspense fallback={<Skeleton className="h-64 w-full rounded-lg" />}>
                {/* TopDevicesChart inserted here in Plan 03 */}
                <Skeleton className="h-64 w-full rounded-lg bg-zinc-800" />
              </Suspense>
            </section>

            {/* Device Activity section */}
            <section aria-label="Device Activity" className="mt-8">
              <h2 className="text-lg font-medium text-zinc-200 mb-4">Device Activity</h2>
              <Suspense fallback={<Skeleton className="h-48 w-full rounded-lg" />}>
                {/* DeviceActivityHeatmap inserted here in Plan 03 */}
                <Skeleton className="h-48 w-full rounded-lg bg-zinc-800" />
              </Suspense>
            </section>
          </div>
        </div>
      )
    }
    ```

    Note: The skeleton placeholders in Plan 03 are replaced with real client components.
    This plan intentionally ships static skeletons so the route is immediately accessible
    and the nav link is testable before Plan 03 runs.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>File exists at src/app/dashboard/insights/page.tsx. Compiles without errors. Page exports a default Server Component with two named sections and Suspense boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /dashboard/insights | Protected by existing dashboard auth middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Spoofing | /dashboard/insights route | accept | Route sits under /dashboard which is already protected by session middleware from Phase 1; no additional auth needed at page level |
| T-10-07 | Information Disclosure | Page shell | accept | No sensitive data rendered server-side in this plan — only static headings and skeleton placeholders |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero errors
2. Start dev server, navigate to /dashboard/insights — page loads without error
3. Nav bar shows Dashboard, Firewall, Groups, Insights links
4. Insights link is highlighted (sky-600) when on /dashboard/insights
5. Page displays "Insights" heading and two section headings
</verification>

<success_criteria>
- Nav layout updated with Insights link following established active/inactive pattern
- /dashboard/insights route exists and renders without error
- Page shell has two clearly labelled sections with Suspense boundaries ready for Plan 03 components
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-insights-page/10-02-SUMMARY.md`
</output>
