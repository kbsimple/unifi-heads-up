---
phase: 10-insights-page
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/components/insights/insights-shell.tsx
  - src/components/insights/top-devices-chart.tsx
  - src/components/insights/device-activity-heatmap.tsx
  - src/app/dashboard/insights/page.tsx
autonomous: false
requirements:
  - INS-01
  - INS-02
  - INS-03
  - INS-04

must_haves:
  truths:
    - "Time range tabs (7d / 14d / 30d) appear at top of page and switch without full page reload"
    - "Top Devices shows a horizontal bar chart of up to 20 devices ordered by total bandwidth"
    - "Clicking a device in the bar chart selects it in the Device Activity heatmap below"
    - "Device Activity heatmap shows 24 hour columns with color intensity proportional to avgMbps"
    - "Hours with avgMbps >= 0.5 are visibly distinct from idle hours"
    - "A dropdown selector in Device Activity also allows picking any device independently"
  artifacts:
    - path: "src/components/insights/insights-shell.tsx"
      provides: "Client Component — time range state + layout container"
      exports: ["InsightsShell"]
    - path: "src/components/insights/top-devices-chart.tsx"
      provides: "Client Component — Recharts horizontal BarChart for ranked devices"
      exports: ["TopDevicesChart"]
    - path: "src/components/insights/device-activity-heatmap.tsx"
      provides: "Client Component — 24-column CSS grid heatmap"
      exports: ["DeviceActivityHeatmap"]
    - path: "src/app/dashboard/insights/page.tsx"
      provides: "Updated to mount InsightsShell instead of static skeletons"
  key_links:
    - from: "src/components/insights/insights-shell.tsx"
      to: "src/app/api/insights/top-devices/route.ts"
      via: "SWR fetch with ?days=N param"
      pattern: "useSWR.*top-devices"
    - from: "src/components/insights/insights-shell.tsx"
      to: "src/app/api/insights/device-activity/route.ts"
      via: "SWR fetch with ?mac=XX&days=N params"
      pattern: "useSWR.*device-activity"
    - from: "src/components/insights/insights-shell.tsx"
      to: "src/components/insights/top-devices-chart.tsx"
      via: "selectedMac state prop + onSelectDevice callback"
      pattern: "onSelectDevice"
---

<objective>
Wire the Insights page with interactive client components: a time-range selector that
drives both views, a Recharts horizontal bar chart for ranked devices, and a 24-column
CSS grid heatmap for device hourly activity.

Purpose: Deliver all four INS requirements as a cohesive interactive page.
Output: Three client components + updated page.tsx replacing static skeletons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/dashboard/traffic-chart.tsx
@src/app/dashboard/insights/page.tsx
@.planning/phases/10-insights-page/10-01-SUMMARY.md
</context>

<interfaces>
<!-- API response shapes from Plan 01 -->
```typescript
// GET /api/insights/top-devices?days=N
// Response: TopDevice[]
interface TopDevice {
  mac: string
  totalBytes: number  // sum of rx_bytes + tx_bytes over period
}

// GET /api/insights/device-activity?mac=XX&days=N
// Response: HourlyBucket[]  (always 24 items, hour 0-23)
interface HourlyBucket {
  hour: number      // 0-23
  avgMbps: number
  active: boolean   // avgMbps >= 0.5
}
```

<!-- Recharts horizontal BarChart pattern (adapt from traffic-chart.tsx) -->
```tsx
'use client'
import { BarChart, Bar, XAxis, YAxis, ResponsiveContainer, Tooltip } from 'recharts'
// layout="vertical" gives horizontal bars with device names on Y axis
```

<!-- Existing dark theme tokens -->
// bg-zinc-900, bg-zinc-800, border-zinc-800, text-zinc-100, text-zinc-400
// Active accent: sky-600 (#0ea5e9)
// Heatmap intensity: zinc-800 (idle) → sky-900 → sky-700 → sky-500 (heavy)

<!-- SWR pattern (established in codebase) -->
```typescript
import useSWR from 'swr'
const fetcher = (url: string) => fetch(url).then(r => r.json())
const { data, isLoading } = useSWR(`/api/insights/top-devices?days=${days}`, fetcher, {
  refreshInterval: 0  // insights are historical, no live refresh needed
})
```

<!-- shadcn Select component (available) -->
```tsx
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: InsightsShell + TopDevicesChart</name>
  <files>src/components/insights/insights-shell.tsx, src/components/insights/top-devices-chart.tsx</files>
  <action>
    **InsightsShell** (`'use client'`):
    - Holds two pieces of state: `days: 7 | 14 | 30` (default 7) and `selectedMac: string | null` (default null)
    - Renders a tab/segmented control at the top with three buttons: "7 days", "14 days", "30 days"
      - Active tab: `bg-zinc-700 text-zinc-100`, inactive: `text-zinc-400 hover:text-zinc-200`
      - Use plain `<button>` elements in a flex row with a `bg-zinc-800 rounded-lg p-1` wrapper (no shadcn Tabs needed)
    - Fetches top-devices data via SWR: `useSWR(\`/api/insights/top-devices?days=\${days}\`, fetcher)`
    - When top-devices data loads, if `selectedMac` is null, auto-select the first device (highest traffic)
    - Passes `data`, `isLoading`, and `onSelectDevice` to `<TopDevicesChart>`
    - Fetches device-activity data via SWR only when `selectedMac` is not null:
      `useSWR(selectedMac ? \`/api/insights/device-activity?mac=\${encodeURIComponent(selectedMac)}&days=\${days}\` : null, fetcher)`
    - Passes activity data, isLoading, selectedMac, and a setter to `<DeviceActivityHeatmap>`
    - Switching `days` resets `selectedMac` to null (triggers auto-select of new top device)

    **TopDevicesChart** (`'use client'`):
    - Props: `data: TopDevice[] | undefined`, `isLoading: boolean`, `selectedMac: string | null`, `onSelectDevice: (mac: string) => void`
    - While loading: render `<Skeleton className="h-64 w-full rounded-lg bg-zinc-800" />`
    - When data available: render a Recharts `BarChart` with `layout="vertical"`:
      - `YAxis dataKey="mac"` — device MAC (replace with resolved name if available; for now use last 8 chars of MAC as label)
      - `XAxis` — value axis, tickFormatter converts bytes to human-readable (MB/GB): bytes < 1e9 → `(b/1e6).toFixed(0)+"MB"`, else `(b/1e9).toFixed(1)+"GB"`
      - `Bar dataKey="totalBytes"` — fill `#0ea5e9` (sky-500), cursor pointer, onClick calls `onSelectDevice(entry.mac)`
      - Highlight selected device bar with fill `#38bdf8` (sky-400) using a custom Cell per bar
      - Wrap in `<ResponsiveContainer width="100%" height={Math.max(200, data.length * 40)}>`
      - Standard Tooltip (dark theme matching traffic-chart.tsx)
      - Container: `<Card className="bg-zinc-900 border-zinc-800 rounded-lg"><CardContent className="p-4">`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>InsightsShell manages days/selectedMac state and fetches from both API endpoints via SWR. TopDevicesChart renders a horizontal bar chart with click-to-select. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: DeviceActivityHeatmap</name>
  <files>src/components/insights/device-activity-heatmap.tsx</files>
  <action>
    Create `src/components/insights/device-activity-heatmap.tsx` as a `'use client'` component.

    Props:
    ```typescript
    interface DeviceActivityHeatmapProps {
      data: HourlyBucket[] | undefined   // 24 items, hour 0-23
      isLoading: boolean
      selectedMac: string | null
      allDevices: TopDevice[]            // for the dropdown selector
      onSelectDevice: (mac: string) => void
    }
    ```

    Layout:
    - Header row: label "Device Activity" + a shadcn `<Select>` dropdown listing all devices from `allDevices`
      - Option label: last 8 chars of MAC (same as chart); selected value = selectedMac
      - Selecting from dropdown calls `onSelectDevice`
    - While loading or no selectedMac: show `<Skeleton className="h-48 w-full rounded-lg bg-zinc-800" />`
    - When data available: render a 24-column CSS grid heatmap

    Heatmap grid:
    ```tsx
    <div className="grid grid-cols-24 gap-1">
    ```
    Each cell: a `<div>` with:
    - Fixed size: `h-10 w-full rounded`
    - Background color determined by `avgMbps`:
      - 0 (idle): `bg-zinc-800`
      - 0 < avgMbps < 0.5: `bg-zinc-700` (light activity, below threshold)
      - 0.5 <= avgMbps < 5: `bg-sky-900`
      - 5 <= avgMbps < 25: `bg-sky-700`
      - >= 25: `bg-sky-500`
    - Title attribute: `{hour}:00 — {avgMbps.toFixed(2)} Mbps`

    Since Tailwind v4 does not support `grid-cols-24` out of the box, use inline style:
    `style={{ display: 'grid', gridTemplateColumns: 'repeat(24, minmax(0, 1fr))', gap: '4px' }}`

    Below the grid: a row of hour labels (0, 3, 6, 9, 12, 15, 18, 21) spaced evenly using
    `justify-between` flex, text-zinc-500 text-xs.

    Below labels: a simple legend row:
    `Idle  ▪ Low  ▪ Active  ▪ High  ▪ Peak` with corresponding colored squares.

    Wrap everything in `<Card className="bg-zinc-900 border-zinc-800 rounded-lg"><CardContent className="p-4">`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>DeviceActivityHeatmap renders a 24-column grid with color-coded cells. Dropdown allows device selection. Loading state shows skeleton. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Wire InsightsShell into page and replace skeletons</name>
  <files>src/app/dashboard/insights/page.tsx</files>
  <action>
    Update `src/app/dashboard/insights/page.tsx` to replace the static skeleton content with the
    `<InsightsShell>` client component.

    The page remains a Server Component (no `'use client'`). Replace the entire `<div id="insights-root">`
    block with a single import and render of `InsightsShell`:

    ```tsx
    import { InsightsShell } from '@/components/insights/insights-shell'

    // In the JSX, replace the two static section blocks with:
    <InsightsShell />
    ```

    `InsightsShell` manages its own layout (two sections, time-range tabs) internally.
    The page retains the outer heading block (h1 + subtitle paragraph) — do not remove that.

    Remove the `<Suspense>` wrappers and `<Skeleton>` imports that were scaffolded in Plan 02
    (InsightsShell handles its own loading states internally with skeletons per task 1 and 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>page.tsx imports and renders InsightsShell. No lingering static skeleton placeholders. TypeScript compiles cleanly. Running dev server shows the interactive page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Insights page:
    - Nav link navigates to /dashboard/insights
    - 7d/14d/30d tabs at top switch time range without reload
    - Horizontal bar chart shows top devices by bandwidth (or empty state if Phase 8 has no data yet)
    - Clicking a device bar selects it; heatmap updates below
    - Dropdown selector in heatmap section also allows device selection
    - 24-column heatmap shows color intensity per hour
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Log in, click "Insights" in the nav bar — page must load without console errors
    3. Verify 7d/14d/30d tabs render; click each — URL does not reload (no full page refresh)
    4. If Phase 8 data exists: bar chart shows at least one device; click a bar — heatmap updates
    5. If no Phase 8 data: bar chart shows an empty/no-data state gracefully (no crash, no blank white area)
    6. Verify heatmap shows 24 columns; hover a cell to see tooltip with hour and Mbps
    7. Verify dropdown lists devices and selecting one updates the heatmap
    8. Check browser DevTools network tab: switching time range fires new fetch to /api/insights/... (not a page reload)
  </how-to-verify>
  <resume-signal>Type "approved" if the page works correctly, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (SWR fetches) | Client-side fetches carry session cookie automatically |
| Client state → API params | days and mac values are user-controlled; validated server-side in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Information Disclosure | SWR cache in browser | accept | Data is household network stats, no PII; session cookie required so other users on the same browser session share visibility intentionally |
| T-10-09 | Tampering | days/mac URL params sent by client | mitigate | Server validates in Plan 01 routes; client-side state is display-only |
| T-10-10 | Denial of Service | Rapid tab switching triggers many SWR fetches | accept | SWR deduplicates; rate limit is 10,000 req/min per CLAUDE.md; home network dashboard, low risk |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero errors after all three tasks
2. Human checkpoint: page renders, tabs switch without reload, chart + heatmap display data
3. No console errors in browser DevTools on page load or tab switch
4. Network tab confirms SWR fires new requests on days change (not cached stale data)
</verification>

<success_criteria>
- All four INS requirements addressed: nav link (INS-01), top-devices chart (INS-02), hourly heatmap (INS-03), time-range selector updating both views (INS-04)
- Recharts components are in 'use client' components (not Server Components)
- Device selection flows correctly from bar chart → heatmap and from dropdown → heatmap
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/10-insights-page/10-03-SUMMARY.md`
</output>
