---
phase: 04-enhanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/dialog.tsx
  - src/components/ui/checkbox.tsx
  - src/lib/types/group.ts
  - src/hooks/use-local-storage.ts
  - src/hooks/use-groups.ts
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements:
  - GRUP-05
must_haves:
  truths:
    - "User can install recharts and shadcn components without errors"
    - "Group data type is defined and exported"
    - "useLocalStorage hook reads and writes to localStorage with SSR safety"
    - "useGroups hook provides CRUD operations for groups"
    - "Navigation shows three tabs: Dashboard, Firewall, Groups"
  artifacts:
    - path: "src/hooks/use-local-storage.ts"
      provides: "LocalStorage persistence with SSR safety"
      exports: ["useLocalStorage"]
    - path: "src/hooks/use-groups.ts"
      provides: "Group management CRUD"
      exports: ["useGroups", "DeviceGroup"]
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Navigation with Groups tab"
      contains: "Groups"
  key_links:
    - from: "src/hooks/use-groups.ts"
      to: "localStorage"
      via: "useLocalStorage hook"
      pattern: "useLocalStorage.*groups"
---

<objective>
Install dependencies and create foundation for device groups feature.

Purpose: Set up the infrastructure needed for groups (localStorage persistence, types, navigation) so subsequent plans can build the UI.
Output: Working hooks, types, navigation with Groups tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enhanced-features/04-CONTEXT.md
@.planning/phases/04-enhanced-features/04-RESEARCH.md
@.planning/phases/04-enhanced-features/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/unifi/types.ts:
```typescript
export interface NetworkClient {
  id: string
  mac: string
  displayName: string
  ip: string | null
  lastSeen: Date | null
  isWired: boolean
  isGuest: boolean
  downloadRate: number
  uploadRate: number
  trafficStatus: 'idle' | 'low' | 'medium' | 'high'
}
```

From src/components/dashboard/traffic-badge.tsx:
```typescript
export function TrafficBadge({ status }: TrafficBadgeProps)
// status: 'idle' | 'low' | 'medium' | 'high'
```

From src/app/(dashboard)/layout.tsx:
```typescript
// Navigation tabs pattern with usePathname() for active state
// Link with className conditional: text-sky-600 border-b-2 border-sky-600 (active)
// text-zinc-400 hover:text-zinc-100 (inactive)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create type definitions</name>
  <files>package.json, src/components/ui/dialog.tsx, src/components/ui/checkbox.tsx, src/lib/types/group.ts</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/package.json
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/types.ts (for NetworkClient reference)
  </read_first>
  <action>
    1. Install recharts: `npm install recharts`
    2. Install shadcn Dialog: `npx shadcn@latest add dialog`
    3. Install shadcn Checkbox: `npx shadcn@latest add checkbox`
    4. Create `src/lib/types/group.ts` with the DeviceGroup interface per D-09:
       ```typescript
       // Per D-09: Group data structure
       export interface DeviceGroup {
         id: string
         name: string
         deviceIds: string[]
       }
       ```
    5. Export from a central location or use direct imports.

    IMPORTANT: Run npm commands from /Users/ffaber/claude-projects/unifi-api
  </action>
  <acceptance_criteria>
    - package.json contains `"recharts":` in dependencies
    - File exists: src/components/ui/dialog.tsx
    - File exists: src/components/ui/checkbox.tsx
    - File exists: src/lib/types/group.ts
    - Group file exports `interface DeviceGroup` with id, name, deviceIds fields
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -E '"recharts":' package.json && ls src/components/ui/dialog.tsx src/components/ui/checkbox.tsx src/lib/types/group.ts</manual>
  </verify>
  <done>
    recharts installed, shadcn Dialog and Checkbox components exist, DeviceGroup type defined with id/name/deviceIds fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useLocalStorage hook</name>
  <files>src/hooks/use-local-storage.ts</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/types.ts
  </read_first>
  <action>
    Create `src/hooks/use-local-storage.ts` with a generic localStorage hook per RESEARCH.md Pattern 1.

    Requirements:
    - SSR-safe initialization (check `typeof window === 'undefined'`)
    - Returns `[storedValue, setValue]` tuple
    - setValue accepts both direct value and updater function: `T | ((prev: T) => T)`
    - Sync to localStorage via useEffect on value change
    - Wrap localStorage operations in try/catch for quota errors
    - JSON.stringify/parse for serialization

    Implementation pattern from RESEARCH.md lines 135-173.
  </action>
  <acceptance_criteria>
    - File exists: src/hooks/use-local-storage.ts
    - Exports `useLocalStorage` function
    - Function signature: `useLocalStorage&lt;T&gt;(key: string, initialValue: T)`
    - Returns tuple `[T, (value: T | ((prev: T) =&gt; T)) =&gt; void]`
    - Contains `typeof window === 'undefined'` SSR guard
    - Contains try/catch around localStorage.getItem and setItem
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -E "useLocalStorage|typeof window|localStorage" src/hooks/use-local-storage.ts | head -10</manual>
  </verify>
  <done>
    useLocalStorage hook exists with SSR safety, generic typing, and error handling for localStorage operations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create useGroups hook</name>
  <files>src/hooks/use-groups.ts</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/hooks/use-local-storage.ts (created in Task 2)
    - /Users/ffaber/claude-projects/unifi-api/src/lib/types/group.ts (created in Task 1)
  </read_first>
  <action>
    Create `src/hooks/use-groups.ts` that wraps useLocalStorage for group CRUD operations per GRUP-01 through GRUP-05.

    Requirements:
    - Use useLocalStorage with key 'unifi-device-groups' and initial value `[]`
    - Provide functions:
      - `createGroup(name: string): void` — generates UUID, creates group with empty deviceIds
      - `addGroupDevice(groupId: string, deviceId: string): void` — adds deviceId to group
      - `removeGroupDevice(groupId: string, deviceId: string): void` — removes deviceId from group
      - `deleteGroup(groupId: string): void` — removes entire group
      - `groups: DeviceGroup[]` — current groups array
    - Use React's `useCallback` for stable function references
    - Re-export DeviceGroup type from './lib/types/group'

    Per D-08: LocalStorage persistence via the underlying useLocalStorage hook.
  </action>
  <acceptance_criteria>
    - File exists: src/hooks/use-groups.ts
    - Exports `useGroups` function
    - Exports `DeviceGroup` type
    - Returns object with: groups, createGroup, addGroupDevice, removeGroupDevice, deleteGroup
    - createGroup accepts name parameter (string)
    - addGroupDevice accepts groupId and deviceId parameters
    - removeGroupDevice accepts groupId and deviceId parameters
    - deleteGroup accepts groupId parameter
    - Uses useLocalStorage with key 'unifi-device-groups'
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -E "useGroups|createGroup|addGroupDevice|removeGroupDevice|deleteGroup|unifi-device-groups" src/hooks/use-groups.ts</manual>
  </verify>
  <done>
    useGroups hook provides full CRUD for device groups with automatic localStorage persistence.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add Groups tab to navigation</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/app/(dashboard)/layout.tsx
  </read_first>
  <action>
    Modify `src/app/(dashboard)/layout.tsx` to add a third navigation tab "Groups" per D-01 and D-02.

    Requirements:
    - Add third Link element after Firewall link
    - href="/groups"
    - Text: "Groups"
    - Same active/inactive styling pattern as existing tabs:
      - Active: `text-sky-600 border-b-2 border-sky-600`
      - Inactive: `text-zinc-400 hover:text-zinc-100`
    - pathname check: `pathname === '/groups'`

    Update comment to reflect three tabs per D-01, D-02.
  </action>
  <acceptance_criteria>
    - File src/app/(dashboard)/layout.tsx contains Link with href="/groups"
    - Link text is "Groups"
    - Active styling: text-sky-600 border-b-2 border-sky-600 when pathname === '/groups'
    - Inactive styling: text-zinc-400 hover:text-zinc-100
    - Link appears after the Firewall Link element
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
    <manual>grep -A5 "Groups" src/app/\(dashboard\)/layout.tsx | head -10</manual>
  </verify>
  <done>
    Navigation displays three tabs: Dashboard, Firewall, Groups. Groups link has correct active/inactive styling.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | Client-side storage accessible to any JS on same origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | localStorage groups | accept | Group data is user preference, not security-sensitive. Malicious modification only affects user's own view. |
| T-04-02 | Information Disclosure | localStorage | accept | No PII stored in groups, only device IDs and names. |
</threat_model>

<verification>
- All npm installs complete without errors
- shadcn components exist in src/components/ui/
- useLocalStorage and useGroups hooks are importable
- Navigation shows Groups tab with correct styling
- All existing tests pass
</verification>

<success_criteria>
- recharts in package.json dependencies
- Dialog and Checkbox components exist
- useLocalStorage hook with SSR safety and error handling
- useGroups hook with full CRUD operations
- Groups tab visible in navigation with active state styling
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhanced-features/04-01-SUMMARY.md`
</output>