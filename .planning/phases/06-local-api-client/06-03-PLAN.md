---
phase: 06-local-api-client
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: false
requirements: [LOCAL-01, LOCAL-02, LOCAL-03, LOCAL-04, LOCAL-05]
must_haves:
  truths:
    - "The full vitest suite is GREEN with the undici-based client (`npx vitest run`)."
    - "Running with `UNIFI_MOCK=true` (or via `./dev.sh`) loads the dashboard with mock data — proves LOCAL-05 unchanged."
    - "Running against a real UniFi console (UNIFI_MOCK unset, UNIFI_HOST set to LAN IP, real UNIFI_API_KEY set) loads the dashboard with real device data — proves LOCAL-01 / LOCAL-02 / LOCAL-03 end-to-end."
    - "Toggling a firewall rule in the app changes its state in the UniFi OS admin UI (visual confirmation in browser) — proves LOCAL-04."
    - "No `NODE_TLS_REJECT_UNAUTHORIZED` env var was set in any startup script, .env file, or shell — TLS bypass is scoped only."
  artifacts:
    - path: ".planning/phases/06-local-api-client/06-03-SUMMARY.md"
      provides: "phase verification record — captures the manual UAT outcome and full-suite state"
      contains: "LOCAL-01"
  key_links:
    - from: "running app (server)"
      to: "UniFi console (LAN)"
      via: "https GET/PUT to UNIFI_HOST with X-API-KEY"
      pattern: "X-API-KEY"
    - from: "firewall toggle in browser"
      to: "UniFi OS admin UI rule state"
      via: "PUT /firewall-policies/${id} → console persists → visual confirmation"
      pattern: "firewall-policies/"
---

<objective>
Phase-wide verification gate. After Plan 02 lands the undici client, this plan exercises both code paths end-to-end:

1. Mock path (UNIFI_MOCK=true) — confirms the dev workflow is unchanged from v1.1 (LOCAL-05).
2. Real path (UNIFI_MOCK unset, real UNIFI_HOST + UNIFI_API_KEY) — confirms LOCAL-01, LOCAL-02, LOCAL-03, LOCAL-04 against actual hardware.

Purpose: The unit tests in Plans 01-02 cover the wire-level contract (correct URL, correct headers, correct method/body, scoped Agent). They cannot cover whether the console accepts the request, whether real device data renders correctly, or whether a firewall toggle persists in the UniFi OS admin UI. That requires a human with access to the LAN and admin browser tabs.

Output:
- Confirmed full suite green
- Confirmed mock path still works (LOCAL-05)
- Confirmed real-hardware path works for traffic dashboard, device groups, 24h history, firewall toggle (LOCAL-01..04)
- 06-03-SUMMARY.md recording the verification outcome
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-local-api-client/06-CONTEXT.md
@.planning/phases/06-local-api-client/06-RESEARCH.md
@.planning/phases/06-local-api-client/06-VALIDATION.md
@.planning/phases/06-local-api-client/06-01-SUMMARY.md
@.planning/phases/06-local-api-client/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite and capture green state</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/06-local-api-client/06-02-SUMMARY.md (confirms client.ts rewrite landed)
    - .planning/phases/06-local-api-client/06-VALIDATION.md (sampling rate — full suite at phase gate)
    - vitest.config.ts (test config)
    - package.json (npm scripts)
  </read_first>
  <action>
    Run the full vitest suite and capture the output. This is the phase-gate sample per 06-VALIDATION.md.

    1. Run: `npx vitest run` from project root
    2. Capture stdout/stderr to a temp log
    3. Confirm exit code is 0
    4. Confirm at least these test files were executed (parse the output):
       - `tests/lib/unifi/client.test.ts` (>= 7 tests, all passing)
       - `tests/lib/unifi/index.test.ts` (4 tests, all passing — LOCAL-05 facade smoke)
    5. If any test fails, do NOT proceed to Task 2. Instead, return a structured failure report with the failing test names and stop the wave.

    Also verify no global TLS bypass was introduced anywhere in the codebase:
    - `grep -rn "NODE_TLS_REJECT_UNAUTHORIZED" src/ tests/ scripts/ 2>/dev/null` must return nothing (or only this plan file's mention)
    - `grep -rn "NODE_TLS_REJECT_UNAUTHORIZED" .env.local .env.local.example dev.sh 2>/dev/null` must return nothing
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - The output mentions `tests/lib/unifi/client.test.ts` with at least 7 passing tests
    - The output mentions `tests/lib/unifi/index.test.ts` with at least 4 passing tests
    - The full output shows zero failing tests and zero skipped tests in `tests/lib/unifi/`
    - `grep -rn "NODE_TLS_REJECT_UNAUTHORIZED" src/ tests/ scripts/ dev.sh .env.local .env.local.example 2>/dev/null` returns no matches (proves T-6-01 mitigation phase-wide)
    - `grep -c "new Agent(" src/lib/unifi/client.ts` returns 1 (singleton invariant holds)
  </acceptance_criteria>
  <done>
    Full automated suite green; no global TLS bypass anywhere in the repo; the codebase is ready for live-hardware verification in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — mock path and live-hardware path</name>
  <files>(none — manual verification of running app + UniFi OS admin UI)</files>
  <read_first>
    - .planning/phases/06-local-api-client/06-VALIDATION.md (Manual-Only Verifications table)
    - .env.local (will be temporarily edited for live-hardware path)
    - dev.sh (sets UNIFI_MOCK=true automatically per MOCK-03)
  </read_first>
  <what-built>
    Plan 02 rewrote `src/lib/unifi/client.ts` to use `undici.fetch` against the local UniFi console via `${UNIFI_HOST}/proxy/network/v2/api/site/default/...` with X-API-KEY auth. A scoped `undici.Agent({ connect: { rejectUnauthorized: false } })` handles the console's self-signed cert without affecting other HTTPS traffic. The mock layer (`UNIFI_MOCK=true`) is unchanged — `index.ts` still routes to `mock.ts` when the flag is set.

    What you (the human) need to verify covers the parts that automated tests cannot: actual data flowing from real hardware, and the firewall toggle persisting visibly in the UniFi OS admin UI.
  </what-built>
  <action>
    PAUSE for human verification. Do NOT auto-resolve. Present the verification steps below to the user, wait for their response containing the filled-in template, then proceed to Task 3.

    The verification has three parts (Part A, Part B, Part C). The full instructions are in the `<how-to-verify>` block below — display them verbatim to the user.
  </action>
  <how-to-verify>
    ### Part A — Mock path still works (LOCAL-05) — ~3 minutes

    1. Confirm `.env.local` has `UNIFI_MOCK="true"` (it should, from Plan 01's edit).
    2. Run `./dev.sh` (or `npm run dev`).
    3. Open the app at the URL it prints (typically `http://localhost:3000`).
    4. Sign in with the dev `admin` user.
    5. **Expected:** Dashboard loads with mock devices (the same 6+ mock clients you've seen in v1.1 dev). Firewall page shows the 3+ mock rules.
    6. **Expected:** No errors in the server console — no UNIFI_HOST connection attempts (UNIFI_MOCK short-circuits before client.ts).
    7. Toggle one mock firewall rule in the app — it flips and stays flipped (in-memory mock state persists until server restart per v1.1 design).
    8. Stop the dev server.

    ### Part B — Live-hardware path (LOCAL-01..04) — ~10 minutes

    > **Skip this part if no UniFi console is reachable on the LAN right now.** Mark Part B as `deferred — no hardware available` in the resume signal and move on. LOCAL-01..04 will be verified at the next opportunity (Phase 7 Docker deployment is a natural moment to re-verify).

    1. Edit `.env.local`:
       - Comment out `UNIFI_MOCK="true"` → `# UNIFI_MOCK="true"`
       - Set `UNIFI_HOST="<your console's LAN IP>"` (e.g., `192.168.1.1`, or `192.168.1.1:8443` if the console listens on the alternate port)
       - Set `UNIFI_API_KEY="<real API key from UniFi OS > Settings > API>"`
    2. Run `npm run dev` (do NOT use `./dev.sh` — that re-applies UNIFI_MOCK=true).
    3. Open the app, sign in.
    4. **Verify LOCAL-01:** Dashboard loads with REAL devices from your LAN. You should recognise the device names / MACs. No "Network error" or "UniFi API error: 401/403" toasts.
    5. **Verify LOCAL-02:** Open the server console. There must be NO `UNABLE_TO_VERIFY_LEAF_SIGNATURE` or `self-signed certificate` errors. (If there are, the scoped Agent is not being applied correctly.)
    6. **Verify LOCAL-03:** Confirm at least one device shows a non-Idle status (High/Medium/Low) — proves real `rx_bytes-r`/`tx_bytes-r` are flowing through `transformClient`. Open the device groups view (if configured) and confirm group cards render. Check the 24h traffic history chart loads without error.
    7. **Verify LOCAL-04:**
       - In one browser tab, open the app's Firewall page.
       - In another browser tab, open the UniFi OS admin UI at `https://<UNIFI_HOST>/network/default/settings/firewall/policy` (or wherever your firewall rules live).
       - Toggle a rule (preferably a low-impact rule) OFF in the app — verify the spinner resolves and the toggle stays off.
       - Refresh the UniFi OS admin tab — the rule must show as DISABLED.
       - Toggle the same rule back ON in the app — refresh the admin tab — must show as ENABLED.
    8. Stop the dev server.

    ### Part C — Restore .env.local

    1. Restore `UNIFI_MOCK="true"` in `.env.local` (uncomment).
    2. Replace the real UNIFI_API_KEY back to `"mock-api-key"` if you don't want the real key sitting in the local file.
    3. Set UNIFI_HOST back to a placeholder (`"192.168.1.1"`) or leave the real one — either is fine because UNIFI_MOCK=true short-circuits.

    ### What to report back

    Use this template in your resume signal:

    ```
    Part A (mock path):    [pass | fail — describe]
    Part B (live hardware):
      - LOCAL-01 (real device data): [pass | fail | deferred — no hardware]
      - LOCAL-02 (no TLS errors):    [pass | fail | deferred]
      - LOCAL-03 (statuses + groups + 24h history): [pass | fail | deferred]
      - LOCAL-04 (toggle persists in UI):           [pass | fail | deferred]
    Part C (env restored): [done | not done]
    ```
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — see human resume-signal in plan output"</automated>
  </verify>
  <acceptance_criteria>
    - Human resume-signal received and contains a filled-in verification template
    - Part A status is `pass` (Part A requires no hardware and must always succeed)
    - Part B status for each of LOCAL-01..04 is one of: `pass`, `fail` (with description), or `deferred — no hardware`
    - Part C status is `done` if Part B was attempted (otherwise N/A)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the verification template filled in, or describe what failed and at which step.</resume-signal>
  <done>
    Human has reported the outcome of the mock-path UAT (Part A) and either the live-hardware UAT (Part B) or marked it as deferred. The reported template will feed into Task 3's summary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 06-03-SUMMARY.md recording phase verification outcome</name>
  <files>.planning/phases/06-local-api-client/06-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/06-local-api-client/06-01-SUMMARY.md
    - .planning/phases/06-local-api-client/06-02-SUMMARY.md
    - $HOME/.claude/get-shit-done/templates/summary.md
    - the resume signal text from Task 2 (the human's verification report)
  </read_first>
  <action>
    Create `.planning/phases/06-local-api-client/06-03-SUMMARY.md` recording the phase verification outcome.

    The summary MUST include:

    1. Frontmatter:
       ```yaml
       ---
       phase: 06-local-api-client
       plan: 03
       date: <today>
       type: verification
       ---
       ```

    2. Section "## Automated suite" — confirm `npx vitest run` exit code 0 and the test counts captured in Task 1.

    3. Section "## Mock path verification (LOCAL-05)" — record whether Part A passed/failed/issues observed.

    4. Section "## Live-hardware verification (LOCAL-01..04)" — record the outcome of each of the four sub-items from Part B. If Part B was deferred (no hardware), say so explicitly and note that LOCAL-01/02/03/04 unit-level coverage is in place but end-to-end verification is pending — flag this for Phase 7 (Docker deployment is the next natural moment to re-verify).

    5. Section "## Decisions confirmed" — list which CONTEXT.md decisions (D-01 through D-13) were exercised by the verification (most should be confirmed; D-13 connection-error propagation is hard to verify without inducing a failure).

    6. Section "## Threats mitigated" — list T-6-01 through T-6-06 with their final disposition status.

    7. Section "## Phase gate" — declare the phase complete or partially complete. "Partially complete with deferred live-hardware verification" is an acceptable outcome if Part B was deferred.

    The user's verification template (from the Task 2 resume signal) is the source of truth for sections 3 and 4 — do not invent results.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-local-api-client/06-03-SUMMARY.md && [ $(grep -cE "^## " .planning/phases/06-local-api-client/06-03-SUMMARY.md) -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/06-local-api-client/06-03-SUMMARY.md` exists
    - Contains valid YAML frontmatter with `phase: 06-local-api-client` and `plan: 03`
    - Contains a section header "Automated suite" with the test exit code
    - Contains a section header for mock path verification mentioning LOCAL-05
    - Contains a section header for live-hardware verification mentioning LOCAL-01, LOCAL-02, LOCAL-03, LOCAL-04
    - Contains a section header "Threats mitigated" or "Threat model" listing T-6-01 explicitly
    - Contains a section header "Phase gate" with an explicit complete / partially complete status
    - File length >= 40 lines (proves it's a substantive record, not a stub)
    - Does NOT contain placeholder text like "TBD" or "[fill in]" — every section reflects actual outcomes from Tasks 1 and 2
  </acceptance_criteria>
  <done>
    Phase 6 verification record committed. Future planners (Phase 7) can read this file to know which Phase 6 success criteria are fully verified vs which need re-verification on real hardware.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App server → UniFi console (LAN) | First real-world TLS connection — verifies scoped Agent works against real self-signed cert |
| Browser → app server | Standard JWT-authenticated dashboard / firewall toggle |
| App server → UniFi OS admin UI | Both observed by the human side-by-side during Part B |
| .env.local during Part B | Temporarily holds real UNIFI_API_KEY; restored to mock value in Part C |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | Scoped TLS bypass leaking | mitigate | Task 1 acceptance criterion runs `grep -rn "NODE_TLS_REJECT_UNAUTHORIZED"` across the entire repo and must return zero matches. Singleton Agent invariant (`grep -c "new Agent(" === 1`) re-checked at phase gate. HIGH severity — blocking. |
| T-6-02 | Information Disclosure | Real UNIFI_API_KEY in .env.local | mitigate | Part C of human UAT explicitly restores the placeholder API key. `.env.local` is gitignored (verify in CLAUDE.md / .gitignore) so even a missed restore would not leak to the public repo. MEDIUM severity. |
| T-6-07 | Information Disclosure | Real UNIFI_API_KEY in shell history | accept | If the human exports the key inline (`UNIFI_API_KEY=... npm run dev`), it lands in shell history. Mitigation by convention: edit `.env.local` rather than inlining. LOW severity for a household-internal app. |
| T-6-08 | Denial of Service | Live console reachable but slow | accept | The 10s `AbortSignal.timeout` from Plan 02 covers this. If timeouts fire during UAT, that is itself a finding that LOCAL-03 or LOCAL-04 fail and gets recorded in the summary. LOW severity. |

</threat_model>

<verification>
- Task 1 exit code 0 (full suite green)
- No `NODE_TLS_REJECT_UNAUTHORIZED` anywhere in repo
- Human UAT report captured (Part A + Part B outcomes)
- 06-03-SUMMARY.md exists and is substantive (≥40 lines, all required sections present)
</verification>

<success_criteria>
- LOCAL-01: real device data renders from `${UNIFI_HOST}` (Part B, item 4) — or deferred with explanation
- LOCAL-02: no TLS errors in server console with self-signed cert (Part B, item 5) — or deferred
- LOCAL-03: traffic statuses, device groups, 24h history all functional with real data (Part B, item 6) — or deferred
- LOCAL-04: firewall toggle persists in UniFi OS admin UI (Part B, item 7) — or deferred
- LOCAL-05: mock path unchanged from v1.1 (Part A) — must pass; this requires no hardware
- Phase gate declared in 06-03-SUMMARY.md (complete or partially complete with deferred items called out)
</success_criteria>

<output>
After completion, the file `.planning/phases/06-local-api-client/06-03-SUMMARY.md` is the canonical phase verification record. Phase 7 (Docker deployment) plans will reference this file to know which Phase 6 success criteria still need re-verification on real hardware once the Docker image is running.
</output>
