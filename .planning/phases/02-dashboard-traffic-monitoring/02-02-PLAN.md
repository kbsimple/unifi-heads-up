---
phase: 02-dashboard-traffic-monitoring
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/dashboard/traffic-badge.tsx
  - src/components/dashboard/last-updated.tsx
  - src/components/dashboard/client-card.tsx
  - src/components/dashboard/client-table.tsx
  - src/components/dashboard/empty-state.tsx
  - src/components/dashboard/error-state.tsx
autonomous: true
requirements: [UIUX-01, UIUX-02, UIUX-03]
user_setup: []

must_haves:
  truths:
    - "TrafficBadge shows correct color for each status level"
    - "LastUpdated displays relative time with loading spinner"
    - "ClientCard displays device name, IP, MAC, status, last active"
    - "ClientTable shows all clients in a table on desktop"
    - "EmptyState displays when no clients found"
    - "ErrorState displays when API fails"
  artifacts:
    - path: "src/components/dashboard/traffic-badge.tsx"
      provides: "Traffic status badge with color coding"
      exports: ["TrafficBadge"]
    - path: "src/components/dashboard/last-updated.tsx"
      provides: "Relative timestamp display"
      exports: ["LastUpdated"]
    - path: "src/components/dashboard/client-card.tsx"
      provides: "Mobile card view for clients"
      exports: ["ClientCard"]
    - path: "src/components/dashboard/client-table.tsx"
      provides: "Desktop table view for clients"
      exports: ["ClientTable"]
    - path: "src/components/dashboard/empty-state.tsx"
      provides: "No devices placeholder"
      exports: ["EmptyState"]
    - path: "src/components/dashboard/error-state.tsx"
      provides: "API error display with retry"
      exports: ["ErrorState"]
  key_links:
    - from: "src/components/dashboard/client-card.tsx"
      to: "src/components/dashboard/traffic-badge.tsx"
      via: "import TrafficBadge"
      pattern: "TrafficBadge"
    - from: "src/components/dashboard/client-table.tsx"
      to: "src/components/dashboard/traffic-badge.tsx"
      via: "import TrafficBadge"
      pattern: "TrafficBadge"
---

<objective>
Create all dashboard UI components for displaying network clients with traffic status.

Purpose: Build the presentational components that ClientList will compose.
Output: TrafficBadge, LastUpdated, ClientCard, ClientTable, EmptyState, ErrorState components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md
@.planning/phases/02-dashboard-traffic-monitoring/02-UI-SPEC.md
@.planning/phases/02-dashboard-traffic-monitoring/02-01-SUMMARY.md
@src/lib/unifi/types.ts
@src/components/ui/badge.tsx
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/components/ui/alert.tsx
@src/components/ui/skeleton.tsx
</context>

<interfaces>
<!-- Types the executor needs from Plan 01 -->

From src/lib/unifi/types.ts:
```typescript
export interface NetworkClient {
  id: string
  mac: string
  displayName: string  // per DEVI-03: name ?? hostname ?? mac
  ip: string | null
  lastSeen: Date | null  // per DEVI-04
  isWired: boolean
  isGuest: boolean
  downloadRate: number  // bytes per second
  uploadRate: number
  trafficStatus: 'idle' | 'low' | 'medium' | 'high'  // per DEVI-02
}

export interface ClientsResponse {
  clients: NetworkClient[]
  timestamp: number
}
```

From src/lib/definitions.ts:
```typescript
export const ERROR_MESSAGES: Record<string, string>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TrafficBadge and LastUpdated components</name>
  <files>src/components/dashboard/traffic-badge.tsx, src/components/dashboard/last-updated.tsx, tests/components/dashboard/traffic-badge.test.tsx, tests/components/dashboard/last-updated.test.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/.planning/phases/02-dashboard-traffic-monitoring/02-UI-SPEC.md
    - /Users/ffaber/claude-projects/unifi-api/.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md (Pattern 4)
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/badge.tsx
  </read_first>
  <behavior>
    - Test 1: TrafficBadge renders "Idle" with gray styling
    - Test 2: TrafficBadge renders "Low" with green styling
    - Test 3: TrafficBadge renders "Medium" with yellow styling
    - Test 4: TrafficBadge renders "High" with red styling
    - Test 5: LastUpdated renders "just now" for recent timestamp
    - Test 6: LastUpdated shows spinning icon when loading
  </behavior>
  <action>
Create TrafficBadge and LastUpdated components following UI-SPEC.md and RESEARCH.md patterns.

1. Create src/components/dashboard/traffic-badge.tsx:
   - Props: { status: 'idle' | 'low' | 'medium' | 'high' }
   - Use Badge component from shadcn with variant="outline"
   - Color mapping per UIUX-02 and UI-SPEC.md:
     - idle: bg-zinc-500/20 text-zinc-400 (gray)
     - low: bg-green-500/20 text-green-400 (green)
     - medium: bg-yellow-500/20 text-yellow-400 (yellow)
     - high: bg-red-500/20 text-red-400 (red)
   - Labels: "Idle", "Low", "Medium", "High"
   - Padding: px-2 py-0.5, font-medium, text-xs
   - Follow RESEARCH.md Pattern 4 exactly

2. Create src/components/dashboard/last-updated.tsx:
   - 'use client' directive
   - Props: { date: Date; isLoading?: boolean }
   - Display format: "Last updated: {relative_time}"
   - Relative time logic:
     - < 60s: "just now"
     - < 1h: "{minutes}m ago"
     - >= 1h: "{hours}h ago"
   - Loading indicator: RefreshCw icon with animate-spin (12px)
   - Update every 60 seconds via useEffect interval
   - Follow RESEARCH.md Code Examples section

3. Create tests verifying color classes and labels.

Per UI-SPEC.md: Traffic Badge uses outline variant, padding px-2 py-0.5, text-xs font-medium.
  </action>
  <verify>
    <automated>pnpm test:run -- traffic-badge.test.tsx last-updated.test.tsx 2>&1 | grep -E "(PASS|Tests:.*passed)" || echo "Tests not passing"</automated>
  </verify>
  <done>
    - TrafficBadge renders all 4 status levels with correct colors (UIUX-02)
    - LastUpdated shows relative time and loading spinner (UIUX-03)
    - All component tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ClientCard, ClientTable, EmptyState, ErrorState</name>
  <files>src/components/dashboard/client-card.tsx, src/components/dashboard/client-table.tsx, src/components/dashboard/empty-state.tsx, src/components/dashboard/error-state.tsx, tests/components/dashboard/client-card.test.tsx, tests/components/dashboard/client-table.test.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/.planning/phases/02-dashboard-traffic-monitoring/02-UI-SPEC.md
    - /Users/ffaber/claude-projects/unifi-api/.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md (Pattern 5)
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/card.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/alert.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/ui/button.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/traffic-badge.tsx (from Task 1)
  </read_first>
  <behavior>
    - Test 1: ClientCard displays device name, IP, MAC, and badge
    - Test 2: ClientCard shows "No IP" when ip is null
    - Test 3: ClientCard shows last active time
    - Test 4: ClientTable renders table with headers
    - Test 5: ClientTable renders multiple client rows
    - Test 6: EmptyState shows "No devices found" message
    - Test 7: ErrorState shows error message with retry button
  </behavior>
  <action>
Create client display components following UI-SPEC.md layouts.

1. Create src/components/dashboard/client-card.tsx:
   - Props: { client: NetworkClient }
   - Card with zinc-900 bg, zinc-800 border, rounded-lg
   - Content padding: p-4 (16px)
   - Layout (per UI-SPEC.md Client Card section):
     - Top row: Device name (font-medium, zinc-100) + TrafficBadge
     - Second row: IP - MAC (text-sm, zinc-400)
     - Bottom section: "Last active: {time}" (text-xs, zinc-500)
   - Handle null IP: display "No IP"
   - Use formatDistanceToNow or custom relative time for lastSeen
   - Follow RESEARCH.md Pattern 5 exactly

2. Create src/components/dashboard/client-table.tsx:
   - Props: { clients: NetworkClient[] }
   - Table with columns per UI-SPEC.md:
     - Device Name (flex-1, left)
     - IP Address (140px, left)
     - MAC Address (160px, left)
     - Status (100px, center)
     - Last Active (120px, right)
   - Header: text-xs uppercase, zinc-500
   - Row height: 48px, border-b zinc-800
   - Row hover: zinc-800/50 background
   - Use TrafficBadge in Status column

3. Create src/components/dashboard/empty-state.tsx:
   - Centered container with p-8
   - WifiOff icon (32px, zinc-500)
   - Title: "No devices found"
   - Description: "Your network may be empty, or there was an issue loading devices."
   - Follow UI-SPEC.md Empty State section

4. Create src/components/dashboard/error-state.tsx:
   - Use Alert component with variant="destructive"
   - AlertCircle icon (16px)
   - Title: "Unable to reach network service"
   - Description: "Please check your connection and try again."
   - Retry button: variant="outline", size="sm", RefreshCw icon
   - onRetry prop for manual refresh trigger
   - Follow UI-SPEC.md Error/Offline State section

5. Create tests for all components.

Per UIUX-01: Cards for mobile, table for desktop (responsive switch handled in ClientList).
  </action>
  <verify>
    <automated>pnpm test:run -- client-card.test.tsx client-table.test.tsx 2>&1 | grep -E "(PASS|Tests:.*passed)" || echo "Tests not passing"</automated>
  </verify>
  <done>
    - ClientCard displays all client info correctly (DEVI-01, DEVI-03, DEVI-04)
    - ClientTable shows clients in tabular format (UIUX-01 desktop)
    - EmptyState shows when no clients
    - ErrorState shows on API failure (UIUX-05)
    - All component tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component props | Data from ClientList (already validated by API route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | client-card.tsx | mitigate | React auto-escapes all rendered content; no dangerouslySetInnerHTML |
| T-02-06 | Information Disclosure | client-table.tsx | mitigate | Same - React auto-escapes; device names from API already validated |
</threat_model>

<verification>
1. Run `pnpm test:run` - all tests pass
2. Run `npx tsc --noEmit` - no TypeScript errors
3. Verify all 6 component files exist in src/components/dashboard/
4. Import each component and verify no runtime errors
</verification>

<success_criteria>
- TrafficBadge shows correct color for each status (UIUX-02)
- LastUpdated displays relative time (UIUX-03)
- ClientCard shows device name, IP, MAC, status, last active (DEVI-01, DEVI-02, DEVI-03, DEVI-04)
- ClientTable shows all clients on desktop (UIUX-01)
- EmptyState and ErrorState handle edge cases
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-traffic-monitoring/02-02-SUMMARY.md`
</output>