---
phase: 02-dashboard-traffic-monitoring
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/components/dashboard/client-list.tsx
  - src/app/(dashboard)/page.tsx
  - src/lib/utils/format.ts
autonomous: false
requirements: [DEVI-01, DEVI-02, DEVI-03, DEVI-04, DEVI-05, UIUX-01, UIUX-05]
user_setup: []

must_haves:
  truths:
    - "User can view all network clients with name, MAC, and IP address"
    - "User sees traffic status (High/Medium/Low/Idle) for each client"
    - "Dashboard shows device name with fallback to hostname then MAC"
    - "User sees when each client was last active"
    - "Traffic data refreshes automatically every 60 seconds"
    - "Dashboard works on mobile (cards) and desktop (table)"
    - "Dashboard shows offline state when API fails"
  artifacts:
    - path: "src/components/dashboard/client-list.tsx"
      provides: "Container with SWR polling"
      exports: ["ClientList"]
      min_lines: 80
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Dashboard page with initial data"
      contains: "getUnifiClients"
    - path: "src/lib/utils/format.ts"
      provides: "Relative time formatting"
      exports: ["formatRelativeTime"]
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/unifi/client.ts"
      via: "import getUnifiClients"
      pattern: "getUnifiClients"
    - from: "src/components/dashboard/client-list.tsx"
      to: "src/app/api/clients/route.ts"
      via: "useSWR('/api/clients')"
      pattern: "useSWR.*api/clients"
    - from: "src/components/dashboard/client-list.tsx"
      to: "src/components/dashboard/client-card.tsx"
      via: "import ClientCard"
      pattern: "ClientCard"
    - from: "src/components/dashboard/client-list.tsx"
      to: "src/components/dashboard/client-table.tsx"
      via: "import ClientTable"
      pattern: "ClientTable"
---

<objective>
Integrate all components into the dashboard with SWR polling for real-time updates.

Purpose: Wire the API client, UI components, and dashboard page together with 60-second polling.
Output: Complete dashboard with device list, traffic status, and automatic refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md
@.planning/phases/02-dashboard-traffic-monitoring/02-UI-SPEC.md
@.planning/phases/02-dashboard-traffic-monitoring/02-01-SUMMARY.md
@.planning/phases/02-dashboard-traffic-monitoring/02-02-SUMMARY.md
@src/lib/unifi/types.ts
@src/lib/unifi/client.ts
@src/components/dashboard/client-card.tsx
@src/components/dashboard/client-table.tsx
@src/components/dashboard/traffic-badge.tsx
@src/components/dashboard/last-updated.tsx
@src/components/dashboard/empty-state.tsx
@src/components/dashboard/error-state.tsx
@src/app/(dashboard)/layout.tsx
</context>

<interfaces>
<!-- All types and components from Plans 01 and 02 -->

From src/lib/unifi/types.ts:
```typescript
export interface NetworkClient {
  id: string
  mac: string
  displayName: string
  ip: string | null
  lastSeen: Date | null
  isWired: boolean
  isGuest: boolean
  downloadRate: number
  uploadRate: number
  trafficStatus: 'idle' | 'low' | 'medium' | 'high'
}

export interface ClientsResponse {
  clients: NetworkClient[]
  timestamp: number
}
```

From src/lib/unifi/client.ts:
```typescript
export async function getUnifiClients(): Promise<ClientsResponse>
```

From src/components/dashboard/*:
```typescript
export function TrafficBadge({ status }: { status: 'idle' | 'low' | 'medium' | 'high' })
export function LastUpdated({ date, isLoading }: { date: Date; isLoading?: boolean })
export function ClientCard({ client }: { client: NetworkClient })
export function ClientTable({ clients }: { clients: NetworkClient[] })
export function EmptyState()
export function ErrorState({ onRetry }: { onRetry: () => void })
```

From src/app/(dashboard)/layout.tsx:
```typescript
// Dashboard layout with navigation bar - renders {children}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ClientList with SWR polling</name>
  <files>src/components/dashboard/client-list.tsx, src/lib/utils/format.ts, tests/components/dashboard/client-list.test.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/.planning/phases/02-dashboard-traffic-monitoring/02-RESEARCH.md (Pattern 1)
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/client-card.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/client-table.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/last-updated.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/empty-state.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/error-state.tsx
  </read_first>
  <behavior>
    - Test 1: ClientList renders clients from initialData
    - Test 2: ClientList shows loading skeleton initially
    - Test 3: ClientList renders cards on mobile viewport
    - Test 4: ClientList renders table on desktop viewport
    - Test 5: ClientList shows ErrorState when SWR has error
    - Test 6: ClientList shows EmptyState when clients array empty
    - Test 7: ClientList passes isLoading to LastUpdated
  </behavior>
  <action>
Create ClientList component with SWR polling following RESEARCH.md Pattern 1.

1. Create src/lib/utils/format.ts:
   - formatRelativeTime(date: Date | null): string
   - Returns "just now", "Xm ago", "Xh ago", or "Unknown"
   - Used in ClientCard and ClientTable for lastSeen display

2. Create src/components/dashboard/client-list.tsx:
   - 'use client' directive
   - Props: { initialData: ClientsResponse }
   - Import useSWR from 'swr'
   - SWR configuration:
     - fallbackData: initialData (Server Component data)
     - refreshInterval: 60000 (DEVI-05 - 60 second polling)
     - revalidateOnFocus: true
     - revalidateOnReconnect: true
     - onErrorRetry with exponential backoff (max 3 retries)
   - Fetcher: (url: string) => fetch(url).then(r => r.json())
   - State handling:
     - error: render ErrorState with mutate() for retry
     - isLoading: pass to LastUpdated for spinner
     - data.clients empty: render EmptyState
     - data.clients populated: render cards/table
   - Responsive layout (UIUX-01):
     - Mobile (< md): render ClientCard in space-y-3 div
     - Desktop (>= md), render ClientTable
   - Include LastUpdated component at top
   - Follow RESEARCH.md Pattern 1 exactly

3. Create tests with SWR mocking.

CRITICAL: refreshInterval must be 60000ms (60 seconds) per DEVI-05.
Per RESEARCH.md Pitfall 2: "Keep refreshInterval at 60 seconds minimum; implement exponential backoff on 429 errors."
  </action>
  <verify>
    <automated>pnpm test:run -- client-list.test.tsx 2>&1 | grep -E "(PASS|Tests:.*passed)" || echo "Tests not passing"</automated>
  </verify>
  <done>
    - ClientList uses SWR with 60-second polling (DEVI-05)
    - Responsive layout: cards mobile, table desktop (UIUX-01)
    - Error handling with retry button (UIUX-05)
    - Loading state with skeleton
    - Empty state when no clients
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update dashboard page with initial data fetch</name>
  <files>src/app/(dashboard)/page.tsx, tests/app/dashboard/page.test.tsx</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/app/(dashboard)/page.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/lib/unifi/client.ts
    - /Users/ffaber/claude-projects/unifi-api/src/lib/dal.ts
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/client-list.tsx
  </read_first>
  <behavior>
    - Test 1: Dashboard page renders ClientList with initial data
    - Test 2: Dashboard page calls verifySession
    - Test 3: Dashboard page shows error toast on API failure
  </behavior>
  <action>
Replace dashboard placeholder with real device list implementation.

1. Update src/app/(dashboard)/page.tsx:
   - Server Component (no 'use client')
   - Call verifySession() for auth check (already present)
   - Import getUnifiClients from '@/lib/unifi/client'
   - Import ClientList from '@/components/dashboard/client-list'
   - Fetch initial data: const initialClients = await getUnifiClients()
   - Wrap in try/catch:
     - On error: show error toast via sonner (use toast() from sonner)
     - On success: render ClientList with initialData
   - Page structure:
     - Section title: "Network Clients" (text-2xl, font-semibold, zinc-100)
     - ClientList component below
   - Remove placeholder content

2. Create tests for dashboard page.

The hybrid pattern: Server Component fetches initial data (fast page load), Client Component polls for updates.

Per RESEARCH.md Pattern 1: "Server Component fetches initial data, Client Component polls for updates"
  </action>
  <verify>
    <automated>pnpm test:run -- page.test.tsx 2>&1 | grep -E "(PASS|Tests:.*passed)" || echo "Tests not passing"</automated>
  </verify>
  <done>
    - Dashboard page fetches initial client data server-side
    - ClientList receives initialData and handles polling
    - Error handling shows toast notification
    - All requirements DEVI-01 through DEVI-05 satisfied
  </done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of dashboard functionality</name>
  <files>N/A - manual verification</files>
  <read_first>
    - /Users/ffaber/claude-projects/unifi-api/src/app/(dashboard)/page.tsx
    - /Users/ffaber/claude-projects/unifi-api/src/components/dashboard/client-list.tsx
  </read_first>
  <action>
Manual verification of Phase 2 dashboard features.

This task pauses execution for human to verify:
1. Device list displays correctly (DEVI-01)
2. Traffic status badges with color coding (DEVI-02, UIUX-02)
3. Device name fallback works (DEVI-03)
4. Last active timestamps display (DEVI-04)
5. Auto-refresh every 60 seconds (DEVI-05)
6. Responsive layout: cards on mobile, table on desktop (UIUX-01)
7. Last updated timestamp (UIUX-03)
8. Error state with retry (UIUX-05)
  </action>
  <what-built>
Complete Phase 2 dashboard with:
- Device list showing name, MAC, IP (DEVI-01)
- Traffic status badges with color coding (DEVI-02, UIUX-02)
- Device name fallback to hostname then MAC (DEVI-03)
- Last active timestamps (DEVI-04)
- Auto-refresh every 60 seconds (DEVI-05)
- Responsive cards on mobile, table on desktop (UIUX-01)
- Last updated timestamp (UIUX-03)
- Offline error state with retry (UIUX-05)
  </what-built>
  <how-to-verify>
**Step 1: Start development server**
\`\`\`bash
cd /Users/ffaber/claude-projects/unifi-api
npm run dev
\`\`\`

**Step 2: Login to dashboard**
- Visit http://localhost:3000/login
- Login with your credentials
- You should be redirected to / (dashboard)

**Step 3: Verify device list displays**
- Check that "Network Clients" heading is visible
- If you have UNIFI_CONSOLE_ID and UNIFI_API_KEY set, devices should appear
- Each device should show: name, IP, MAC, traffic badge, last active time

**Step 4: Verify responsive layout**
- On desktop (or resize browser > 768px): Table view
- On mobile (resize browser < 768px): Card view

**Step 5: Verify traffic badges**
- Idle: Gray badge
- Low: Green badge
- Medium: Yellow badge
- High: Red badge

**Step 6: Verify auto-refresh**
- Watch "Last updated" timestamp
- Wait 60 seconds - should update automatically
- Loading spinner should briefly appear during refresh

**Step 7: Verify error handling**
- Temporarily break UNIFI_API_KEY in .env.local
- Restart dev server
- Should see error state with "Retry" button
- Click retry - should attempt to reload

**Step 8: Restore environment**
- Fix UNIFI_API_KEY
- Restart dev server if needed
  </how-to-verify>
  <verify>
    <automated>N/A - human verification checkpoint</automated>
  </verify>
  <done>All 8 verification steps pass and user types "approved"</done>
  <resume-signal>Type "approved" if all verification steps pass, or describe issues found</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API Route /api/clients | Session required, rate limited |
| Server Component → UniFi API | Server-only, credentials protected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service | client-list.tsx | mitigate | SWR deduplication prevents request spam; 60s interval |
| T-02-08 | Information Disclosure | page.tsx | mitigate | API errors logged server-side, generic message shown to user |
</threat_model>

<verification>
1. Run `pnpm test:run` - all tests pass
2. Run `npx tsc --noEmit` - no TypeScript errors
3. Run `npm run build` - production build succeeds
4. Manual verification via checkpoint
</verification>

<success_criteria>
- User can view all network clients with name, MAC, IP (DEVI-01)
- Traffic status shows correctly colored badges (DEVI-02, UIUX-02)
- Device names fall back correctly (DEVI-03)
- Last active timestamps display (DEVI-04)
- Auto-refresh works every 60 seconds (DEVI-05)
- Responsive layout works on mobile/desktop (UIUX-01)
- Last updated timestamp shows (UIUX-03)
- Offline state shows with retry (UIUX-05)
- All tests pass
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-traffic-monitoring/02-03-SUMMARY.md`
</output>